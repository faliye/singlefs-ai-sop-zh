# CHANGELOG

规则与门禁的版本历史。`CLAUDE.md` 与 `rules/*.md` 不留历史节（design-doc-discipline），
历史一律记在这里；逐条改动细节见 `git log`，提交信息即变更说明。

## 0.0.40 — 2026-09-10

**新增门禁阶段「CHANGELOG 连续」（`scripts/changelog-lint.sh`）：每一版都要有自己的一节，最新一节就是 VERSION。**
版本纪律只管「改了规范本体就抬 VERSION」，不管 CHANGELOG 跟没跟上。实测：0.0.39 那次 VERSION 从 0.0.35
抬到 0.0.39，三个语言仓的 CHANGELOG 一起跳过了 0.0.36，那一版的两处改动没有任何一节记着；全部门禁照绿，
是逐段对 diff 才看出来的。

判的是整份文件，不是 diff 窗口：二级标题只许是 `## x.y.z — YYYY-MM-DD`，最后一节可以是「x.y.z 及更早」
这种不带日期的收尾；最新一节等于 VERSION；相邻两节必须是紧后一版（补丁 +1、次版本 +1 且补丁归 0、
主版本 +1 其余归 0），跳号、重复、倒序都红；一节都没有也红。只在 SOP 仓自己跑，各语言仓各判各的 CHANGELOG。
`CLAUDE.md` 首屏补了一句，`skills/gate/SKILL.md` 的「只在 SOP 仓自己跑的阶段」加上了它。

## 0.0.39 — 2026-09-10

**`rules/verify-before-claiming.md` 新增一节：核了窄的那一句，说出口的却是宽的那一句。**

原来那一篇管两件事：说外部状态之前现查；以及「它定了没」和「它到底怎么定义的」是两个问题。
两条都假定**没查**是失败模式。这一节管的是**查了也失败**的那一种——
你确实跑了命令、读了文件，手上是一条被核实的命题，**错在说出口的那句比它宽**。

判据：说出口的那句话，它的主语和范围，是不是就是刚才核的那一个？
核的是「这条路不通」，只能说「这条路不通」，不能说「做不到」；
核的是「这个文件里没写」，只能说「这个文件里没写」，不能说「全仓没有」。
做法是把那句宽的话当成待证命题，问「要让它为真，需要排除掉哪些情况」，逐个排除；
排不完就把话缩回真排完的那个范围。

实测于 singlefs（2026-09-10，同一天同一个人两处）：
① 核到「`sudo` 被沙箱拦下」⇒ 写下「拿不到 root，这个观测做不了」，
而同一个仓里另一条路**不需要那个权限**、文档里逐字写着「已实测通过」
⇒ 整轮零观测，而记录在案的理由是假的。
② 核到「某条类规则的枚举里没列这个对象」⇒ 写下「这一格全仓没有落点」，
并据此立了一整个实验的题；而**类归属写在另外两个文件里**，那条类规则本来就罩着它
⇒ 立题作废，同一笔欠账连写错两次。

⇒ 该节末尾另附一条：「一个对象有没有被条款罩着」尤其容易中招，
因为**条款可以写在别处、按类罩着它**，而它自己那一节一个字都没有；
判它之前拿它的名字全仓搜一遍，看它有没有被归进某个已有的类。

## 0.0.38 — 2026-09-10

**两条各自补在已有的一节里，都是「条款还在、但已经不起作用」这一族。**

**`rules/evidence-discipline.md`「重跑之后要回对正文」补第二种形态：不是数漂了，是限定词没了。**
原来那一节只管数值漂移——正文写 1.475× 而产物是 1.625×，区间断言罩不住。
但正文与产物对不上还有一种：**每个数都是真的，而那句结论比产物支持的范围宽**，
因为正文那张表把一个参数维度整个脱掉了。数漂了还有两个数可以并排对，
限定词没了**连对照物都没有**。判据是：结论说「只有 A 才买得到」时，
去产物里读**非 A 的臂在每一个参数点上的值**；正文那张表没有那个参数的列，那句「只有」就不成立。

实测于 singlefs（2026-09-09）：一份实验正文写「真正只有聚集才买得到的是回收那一格……
另两条臂一个都腾不出来」，而同一份留存产物里另一条臂在不交错那一格上与它**逐格相同**；
正文那张表没有交错步长这一列。**装置的断言射程一直是对的**——单测写死了那个参数，
断言消息里就带着「交错之后」——脱掉射程的是正文。复跑逐字节一致、**门禁全绿**。
那句结论已被三处下游继承，其中一处是靠它定案的那条决策分项本身。

**`rules/test-discipline.md`「失败条款不许让结论不可证伪」补反面：前件可以写反，写反之后永不发火。**
主结论管的是「条款让结论不可推翻」；这一条管的是「条款自己不可能被触发」。
写反不长成「错了」的样子，长成「**没触发**」的样子——而在轮次报告里，
「这条条款没触发」与「这条条款检查过、结论没问题」看不出差别。
做法：**每写一条失败条款，紧接着写一句「什么观测会让它触发」**；写不出就是写反了或写空了。

实测于 singlefs（2026-09-09）：一条跑前写死的条款写「某条臂的收益只在**不交错**负载上出现
⇒ 记条件未知」，而那一轮的发现是收益只在**交错**负载上出现 ⇒ 前件恒假、一次没触发，
而它本该触发。写条款的人自查两遍都没看出来，是另一条腿逐字核前件时打中的。

## 0.0.37 — 2026-09-09

**`rules/evidence-discipline.md`「不许先有结论再建模型」补一节：臂的定义也在「跑前写死」之列，
以及失败条款打中时的唯一合法出路。**

此前的条款只给了禁令——判据、阈值、作废条款跑前写死，「不许回头改判据」「不许事后把规则改宽
再宣布它赢」——但**没给合法出路**。于是失败条款真的打中时，只剩两条路：装作没触发，或整轮作废。
而臂本身写含糊时，攻击会打中它**最弱的读法**，顺手把臂「澄清」成强的那个再宣布它赢，
形式上判据一个字没改，实质是事后建模。

补的三步是：按最弱读法**记一次输**、新形态**只许收严**（要被原来那套判据判得更严或相同）、
**写明收严在哪**（说不出新形态多要求了什么，那就是放宽）。并点明第一步最容易省，
而省掉之后，诚实的收严与「改宽再宣布赢」在文本上长得一模一样。

实测于 singlefs（2026-09-09）：一轮三方论证里，反推腿按跑前写死的失败条款判一条臂出局，
理由是它罩不住「字段顺序」。核下来那条臂的原文并没有说过它只投影标量——打中的是最弱读法。
判决按三步走完，收严后的形态写进了那条决策正文。

## 0.0.36 — 2026-09-09

**`rules/evidence-discipline.md`「撤回一个数或一条结论，同样要当场回扫谁在引它」之后补一节反面：
撤回的理由失效，不等于被撤回的结论自动回来。** 撤回是一次判决，理由塌了只说明这次判决的依据没了，
不说明反面成立，要让原结论回来得重新论证。而且撤回之后那一格通常已经有另一条依据顶上来，
只盯着被撤回的那条会把它整个漏掉。做法是按顺序问三句：今天顶在这一格上的是哪条依据、
它自己站不站得住、复活所需的重新论证做了没有。

**`skills/decide/SKILL.md` 新增第 7 条：待定项的问法必须只有一种读法。**
把每一种读法各写一遍，答案不一样就是问法还没定完——先定问法，再谈依据。

实测于 singlefs（2026-09-09），两条出自同一格：一条依据 2026-09-06 因「与 X 互斥」被撤回，
X 次日取消，提问者据此要求重判，方向指着复活。三方论证的反推腿打中的三处，都与「互斥还在不在」无关：
另一份文档当天就留了一条不依赖那个互斥的替代依据；那条替代依据的两个支点各断一处；
而这一格问的根本不是被撤回那条依据回答的事——问法「要不要同等完整性」有两种读法
（问它自己的字节，还是问它指向的目标），被撤回的依据答的是前一种，措辞问的是后一种。
那一格卡了三天、走了两轮论证，问法定死之后当场收口。

## 0.0.35 — 2026-09-06

**`rules/show-me-test.md`「踩过的坑要做成会失败的检查」补一节射程：立在一个装置里的检查，
管不住下一个装置。** 断言与变异测试只在它所在的那套装置里生效；换一套装置把同一件事
重新建一遍模型，那条检查一个字都不会说。

实测于 singlefs（2026-09-06）：一个计数模型把「容量 × 填充率」那笔预算当成其中一类对象的数量、
再往上加另一类，等于往盘上摆了 126% 的对象；当天修好并留了会红的断言。几小时后另一个模型
重犯同一个错（135%），那条断言在别的装置里，一声没吭——新装置自己单测全绿、变异全抓、门禁全绿，
两个模型对同一个物理量报的数差 1.5 倍，没有任何东西去对过，错的那个数还写进了一条已定条款。

判据因此从「这个坑做没做成会红的检查」改成「**这个坑在哪一层**」：坑在一段代码的行为里，
检查立在代码旁边就够；坑在**建模口径**上（同一个量该怎么算），就必须立成**装置之间的交叉断言**。
快速判据是「换一套装置从头再做一遍，还会不会再踩」。并点明修坑的人最容易在这里收手——
他确实把坑做成了会红的检查，证据齐全、门禁全绿，所以不会再想一遍那条检查的射程。

**`rules/evidence-discipline.md`「引产物就整行抄」补一节：重跑之后要回对正文，复跑绿不代表正文对。**
复跑钉的是结论落在哪个区间，区间有多宽，正文就能在里面漂多远。实测：一份实验正文写
1.475× / 22.33× / 5 123 506 ns，而它自己的留存产物逐字是 1.625× / 30.36× / 5 199 857 ns，
三个数全在区间断言之内，门禁全绿。同节顺带管住「N 个单测 / M 条变异 / 产物 K 行」这类
可以被一条命令数出来的声明——同一天在同一个仓里查出五处与源码或产物对不上。

两处都只加规则本体，没有新增门禁阶段：跨装置交叉断言要先给产物定一套「这个数是什么量」的
机器可读标注，本包里没有；计数声明的机检落点是各自项目的本地阶段。

## 0.0.34 — 2026-09-06

**`rules/evidence-discipline.md`「新立一条判据就回扫已有条目」补一节：撤回一个数或一条结论，
同样要当场回扫谁在引它。** 同一条纪律换个对象，但更隐蔽——撤回的人通常确实扫了几处，
于是有理由认为扫完了，漏掉的那处要过几天才发作。

实测于 singlefs（2026-09-06）：一个位置条目的宽度被改写时，那条决策正文自陈「本项定的是
一个此前从没走过决策流程、却已经被消费掉的数」，并当场点名扫了下游两个派生数；三天后另一份
文档里第三处引用仍写着旧值，被抄进一次三方论证的背景材料，三条腿一起继承，凡踩在那个数上的
论证整轮作废。撤回的人扫了两处、漏了一处，而他并不知道自己漏了。

判据写成「撤回之后答得出一份『全仓还有谁在用这个数』的清单才算撤完」，并点明扫描范围是全仓
不是记忆——记忆挑出来的正是好找的那几处。同时写明它与既有的「喂给多方论证的背景材料本身要先核」
是同一个洞的两端：一端是引的人没现查，另一端是撤的人没扫干净，两端都堵才堵得住。

规则本体只加一节，没有新增门禁阶段：这条今天还没有可机检的形态（要判「这个数被撤回了」需要
一份撤回登记，本包里没有）。使用方要机检的话，落点是各自项目的本地阶段。

## 0.0.33 — 2026-09-06

**`install.sh` 学会分辨「项目接管了这份文件」和「项目落后于上游」。** 此前它只判
「与上游模板不同」，两种情形一模一样。而 kb 骨架、skill 桩本来就是给项目改的——
于是**任何一个动过自己 kb 的项目，装完第一次之后版本戳就再也刷不动了**，
门禁阶段 0 从此长红。实测于 singlefs：12 份「落后」全是项目自己改的
（`.claude/kb/INDEX.md` 有 36 行是项目自己的写法）。

项目把接管的那几份写进 `$ROOT/.claude/install-owned`，一行一条 `<相对路径>  # 为什么`。
**理由不许省**：接管一份文件的代价是此后上游对它的改动都不会再送到，
理由那一栏就是让人正面对上这一点。清单一律报进输出——静悄悄少比几份，
和这道守卫没实现长得一模一样。

两条会红：没写理由；写了 `install.sh` 根本不铺的路径（那种条目一点作用都没有，
却让人以为那份文件已经被豁免了）。**清单坏了当场挡住，不许接着刷版本戳。**

`README.md` 里「版本戳每次都刷新」那句是错的，一并改掉——它和 `install.sh` 的实际行为
早就对不上了。

5 个变异逐个跑过。其中一个暴露了盲区：把「清单坏了就挡住」整段删掉，原有两个样本
**一起全绿**——它们同时还有内容落后，是那条路让它们红的。补了一个没有别的落后的场景
才隔离出来。自检 182 → 188 例。

## 0.0.32 — 2026-09-06

**gate-lint 补上第三种拒绝形态：直接打印的 `✗`。** 此前它只认 `lib.sh` 的 `bad` 与 `die`，
而**项目本地的阶段多半不 source `lib.sh`**——它们直接 `echo "  ✗ …"`，或者把判据写在
内嵌的 python 里 `print('  ✗ …')`。这一整类拒绝一条都没被查过。

实测于 singlefs 的本地阶段：46 处这样的拒绝全部免检，补上检查后 **14 处一句下一步都没有**
（`15-research-build.sh` 的「没有 cargo」、`70-citations.sh` 的「找不到 $S」这种，
都是直接 `exit 1` 而不告诉人怎么办）。gate-lint 覆盖的拒绝数 125 → 170。

两处判据是照真实语料定的：

- **窗口不按 5 行算，按「到下一处拒绝之前」算。** python 常常先打一句 `✗`、再循环列明细、
  最后才给出路，5 行窗口会把这一批整体误判成没出路（样本 `printrejok` 盯着它）。
- **heredoc 体默认不当代码读，但解释器 heredoc 要读。** `python3 - <<'PY'` 里的 `print`
  是真的会打给提交者看的；照旧跳过的话，python 写的阶段仍然整体失明
  （样本 `printrejpy` 盯着它）。

两种豁免都要显式写出来，猜不着的东西不猜：循环里逐条列明细的行标 `# gate-lint:detail`，
汇总行标 `# gate-lint:summary`。

`rules/sop-first.md` 的拒绝形态表加了第三行。样本 4 个，4 个变异逐个跑过，
每个恰好被一个用例抓到。自检 178 → 182 例。

⚠️ **这条会让 singlefs 的门禁红 14 处**，都在它自己的 `.claude/gate.d/` 里，
上游三个仓不受影响（共享包里一处直接打印的拒绝都没有，全走 `bad`）。

## 0.0.31 — 2026-09-06

**新增规则 `rules/pushback-discipline.md`：提议不因为来自谁而免检。** 用户提的方案，
和别处来的方案过同一道闸。和实测数据、或者已经核实过的事实对不上时，动手之前就要说清
三样：对不上的是哪一条（带出处）、按提议做会出什么问题（什么现象说明它出现了）、
有没有第三条路。对方明确重申就照做、不再劝、不许消极执行，但**警告必须留下来**。
**做完了才发现对不上，照样要说，该撤就整个撤回**——成本由用户承担，代码只对正确性
负责，「已经做了这么多」不是继续的理由。代码改动里没有撤不回来的事：撤回的代价过去是
人的时间，现在那部分是机器做的，几万行和几百行没有量级差别。真正退不回去的是
`git checkout`、`rm -rf` 与对外的永久承诺，`command-safety.md` 管那一类。

**警告落在 `.claude/warnings/<日期>.md`**，一天一个文件，一次警告一个 `##` 小节，
四项齐全：提议 / 异议 / 已知风险 / 结果。别处一律链过来，不抄第二份
（`kb-discipline.md` 第 4 条）。按日期分文件是为了**不用回头改旧文件**——警告写下就是
当时的记录。`install.sh` 会在项目里铺出这个目录。

**doc-lint 新增两项检查**：

- `.claude/warnings/` 下的文件名必须是 `YYYY-MM-DD.md`，每个 `##` 小节四项要齐。
  项名按语言取，三种语言都跑：它认的是固定项名，不是靠字形黑名单猜语义，
  所以不进「没有词表就未实现」那一档。
- `rules/*.md` 与 `CLAUDE.md` 里的 `@rules/` 引用必须逐项相等。两个方向都无声：
  没被引用的规则不会进上下文，却和生效的规则长得一模一样；引用了不存在的文件，
  那一行只是不展开，CLAUDE.md 读起来照样完整。这条正是这一轮自己需要的——
  新加一份规则而忘了写进 CLAUDE.md，此前一点也不会红。

样本 7 个；5 个变异逐个跑过，每个恰好被一个用例抓到。自检 171 → 178 例。

## 0.0.30 — 2026-09-06

**en / ja 两仓的门禁修红。** 两处，都是「一份规则复制成多语言仓」漏掉的口子：

- `doc-lint` 的样本是中文写的，而 `scripts/` 逐字节复制进每个语言仓。于是在 en 仓里，
  这批样本被拿去跟 `## Revision history` 对判——44 例一起假红，而 doc-lint 自己一个字都没坏。
  语言是**样本的属性**，不是仓的属性：`selftest` 现在用 `DOC_LINT_LANG=zh` 钉住样本的语言。
  这个口子能换掉判据，所以**设了就打印出来**，并有一条自检盯着那句话还在不在。
  0.0.25 按语言取判据时留下的，en / ja 从那时起一直红着。
- en 仓把 `templates/kb/` 里 D1 与 E1 的占位简称都译成了 `<name, 24 characters or fewer>`，
  撞在「一个简称不许两个编号共用」上。改成各带名词；zh / ja 的译法本来就是分开的。

**doc-lint 新增排除清单：原样保存的证据不许事后改。** `research/prompts/` 这类目录存的是
当时原样发给模型的提示，与产物一一对应，事后改一个字，产物就不再对应它的输入。
项目在 `.claude/doc-lint-exclude` 里声明，一行一条、**每条要写明为什么**；指向不存在的目录、
或者一个文件都没排到，都判红——不起作用的排除项会让人以为那批文件已经被绕开了。
排除项一律报进输出：静悄悄少扫两百个文件，和这道检查没实现长得一模一样。
规则见 `rules/evidence-discipline.md`；七个样本，含一个不带排除声明的对照组。

## 0.0.29 — 2026-09-06

- `doc-lint.sh`：「已被 X 覆盖」这条的编号前缀从任意大写字母收窄到 `D/E/C/I/A/O`。
  实测误判：一句讲盘上字节的话「要重建 U1，需要 U2 的原始字节 —— 已被 U9 覆盖」被判成
  「正文不许就地标注被某条覆盖」。那句里的「覆盖」是字节被写掉，不是条款被推翻，
  两个义项撞在同一个词上，只能靠「被覆盖的那个东西是不是一条编号条款」来分。

## 0.0.28 — 2026-09-05

**计数判据认 python 的 f-string。** 只认 shell 的 `$n` 时，用 python 实现的阶段
整批假红——它们的成功摘要在 python 里格式化（singlefs 的两个阶段实测）。
样本 `countpy`；`nocount` 仍红，判据没有被掏空。

## 0.0.27 — 2026-09-05

**修 0.0.26 自己引入的一处无限循环，并给 selftest 加超时保护。**

- `gate-lint` 挖算术展开时，配对的 `))` 只在 `$((` 之后找。在整行里找第一个 `))` 会挂死：
  `if ((okc)); then …; pass=$((pass+1)); fi` 这样的行，第一个 `))` 落在 `$((` 前面，
  切完的串仍含 `$((` 且不变短。真实语料上当场无限循环——跑了 9 分钟没结束，
  既不红也不绿。样本 `arith2` 必须跑得完且是绿的。
- `selftest` 的两处样本执行点加 `timeout`（默认 60s，`SELFTEST_TIMEOUT` 可调）。
  **挂死的检查比红的检查危险**：它在门禁输出里什么都不是，只是永远不回来。

## 0.0.26 — 2026-09-05

**项目本地阶段第一次被门禁自己管起来，并补上三条从 singlefs 三天作业里剥出来的纪律。**

一、**`.claude/gate.d/` 纳入两个 lint**

`gate.sh` 把项目本地阶段目录一并交给 `gate-lint` 与 `shell-lint`。此前它不在任何一个
lint 的扫描里——那些脚本和共享阶段一样会拒绝提交者，却从没被查过：一喂就是 7 条
没有出路的拒绝（singlefs 实测）。

二、**gate-lint 新增第三条：成功摘要要报出检查了多少项**

「扫到 0 项」不是通过。判据的搜索范围写窄一点，对象就全被第一步跳过，
既不算通过也不算失败，末尾照样报绿（singlefs C114：一个阶段的第 3 项就这样绿着）。
只对**报了成功**的脚本判，确实不扫对象的写 `# gate-lint:nocount <理由>`。
样本 `nocount` / `countok`；本包自己的 `show-me-test.sh` 是第一个被它咬到的。

三、**gate-lint 修一处假红**：`bad=$((bad + 1))` 里的 `bad ` 紧跟 `(`，按 CMD_POS 读
成了一处拒绝。判定前挖掉算术展开；样本 `arith`。

四、规则增补（都带实测代价，各一节或一行）

- `evidence-discipline`：**引产物就整行抄**（同一处差值连错四轮）；
  **新立判据当场回扫已有条目**（尺子只挥向新来的那个）；
  取样表加一行「同一份产物里有没有反例」；稻草人臂那节加「引某条臂的数要带它的定义」。
- `show-me-test`：会红那条**同样管设计论证**——对抗轮打中的每一条，先做成必报非 0 的世界再改规则。
- `skills/decide` 硬要求第 6 条：**动了由人拍板的那一句，要在 `checks-owed.md` 记一笔未还的账**。

⚠️ **本轮发现、未修**：译本仓（en / ja）自己跑 `gate.sh` 不是绿的——`selftest` 有 44 处判错。
`i18n-sync` 只在参照仓跑，译本仓的门禁此前没人跑过。归因已做：拿 0.0.23 的英文 rules
配当前脚本跑是 47 处，比现在还多 ⇒ 与 0.0.26 的改动无关，是包的既有状态。
病灶是 selftest 里那批「拿仓内真实语料喂检查」的用例——它们的判据按中文语料写，
对译本仓成不成立还没判过。修它要先回答这个问题，所以不在本轮顺手改。

## 0.0.25 — 2026-09-02

**译本仓里最后一批中文清掉，`agents/` 这一层立起来。** 两件都落到会红的检查上。

一、**skills / templates 从「原样复制」改成「逐篇翻译」**

此前它们在 `i18n-sync` 的 `SHARED` 里逐字节复制，于是日语仓的 skill 正文、项目骨架、
litmus 注释全是中文——而 `templates/CLAUDE.project.md` 会被 `install.sh` 写成使用者
项目根的 `CLAUDE.md`。与「用日文的不必读中文」直接冲突。

- 清单从 14 条扩到 28 条：`CLAUDE.md`、`rules/`、`agents/`、`skills/`、
  `templates/`（含 `*.litmus`）。判据一条：**里面有没有面向人的散文。**
- **新增覆盖率检查**：两边都不沾的文本当场红，`howto` 逼你二选一。
  这是「空缺」的可机检形态——新加一份 `.md` 而忘了决定它翻不翻译，门禁替你想起来。
- **溯源标记的位置与包裹形式按文件类型定**：`SKILL.md` 的 YAML frontmatter 必须从
  第 1 行起（拍在它前面 skill 安静地装不上）；`.litmus` 第 1 行必须是 `C <名>`，
  herd7 报 `splitter error in sublexer first line`（跑 herd7 实测）。
  盖章后回读确认，并做成会红的检查。
- **`install.sh` 铺进项目时剥掉溯源标记**：那是分发层的账，抄进使用者项目就是一条
  永不更新的陈旧标注，而且贴在他马上要改的文件上（装一遍实测到）。
- 28 篇译文（en/ja 各 14 篇）全部重写并盖章。三语的 litmus 经 herd7 实跑，
  判定一致：Never 0 3 / Sometimes 1 3。

二、**`agents/` 纳入治理**

- `agents/INDEX.md` 写明这一层放什么（与 `skills/` 的分界：读的还是委派的）、
  怎么写、三条纪律、怎么接。进清单、逐篇翻译。
- 进 `GOVERNED`（改了要抬 `VERSION`）、`install.sh` 铺 agent 桩（与 skill 同构）。
- **空是状态，要说出来**：目录为空时 `manifest.sh` 显式报「已纳入治理，当前为空」，
  不静默通过。
- doc-lint 新增：agent 定义不留历史节；必须有 `name`（与文件名一致）与 `description`——
  两样都不会报错，只会安静地不生效。

三、顺带修的

- `manifest.sh` 的 `gen()` 在管道里丢了 `cd`——从仓根跑时 cwd 恰好就是包根，看不出来，
  selftest 拿样本仓一喂就露了。
- `translated_paths` 里 `find` 撞上不存在的目录整条失败：`2>/dev/null` 只藏消息不藏
  退出码，`--update` 退出码 1 而一个字都不打印。
- `stamp_read` 原先用 sed，两种包裹形式要写交替，而 BRE 的 `\|` 跟分隔符撞车——
  它静默匹配不上，靠盖章后的回读断言才抓出来。改用 awk。
- **GLOSSARY 的语言缺口补掉了**：说明列改成三语并列（`zh<br>en<br>ja`）。
  它仍然不翻译——**它本身就是对照表，抄成 N 份必然漂移**；而说明是面向人的散文，
  必须让读它的人读得懂。两件事在同一张表里各按各的规矩办。
  `doc-lint` 判「说明列不是三语」：段数不等于 3、或某段为空，都红。
  样本里四种坏法（1 段 / 2 段 / 3 段但中间空 / 4 段）各踩一条判据，互相盖不住。
  **本包至此没有已知的语言缺口。**

判别力：用例 111 → 120。新加的检查逐个变异验过，改前全绿、改后全红——
其中五条是本轮自己制造的新盲区（覆盖率检查、第 1 行被压住、install 剥标记、
agent 的 name、`GOVERNED` 的 `agents/`），补样本后才咬得住。

## 0.0.24 — 2026-09-02

**这一版没有单独发布过，和 0.0.25 一起在同一次提交里出去。**
下游看到的版本会从 0.0.23 直接跳到 0.0.25，这里记下来免得有人去找 0.0.24。

第二轮对抗审计（变异测试驱动）后的整批修复。**主线是判别力**：上一轮补的门禁里，
有六条删掉之后 selftest 仍然 54/54 全绿——检查在，盯着检查的东西不在。

- **判别力**：doc-lint 的历史陈述模式组 / CLAUDE.md 历史节 / kb 历史节、
  gate-lint 的 howto 窗口值、lkmm 的两条静态检查、show-me-test 的标注广度，
  各配单一职责样本；`blind` 的 `want` 从「正文不许」（六类检查共用）拆成逐条。
  八个变异现在全部转红。用例 54 → 69。
- **doc-lint 的死模式**：`'[〔【\[]已废弃[〕】\]]'` 在 GNU grep 下永不命中——
  POSIX 规定方括号集合内的反斜杠是字面量。改成 `'[[〔【]已废弃[]〕】]'`。
- **die 不再免检**：`die` 是 `bad` + `exit`，同样是拒绝。lib.sh 的 `die` 改成
  「消息 + 出路」两段，gate-lint 判「只带一句话的 die」，17 处调用点补齐出路。
- **新增 shell-lint.sh**：把 command-safety 里两条可机检的做成会红的项——
  按模式匹配杀进程、子 shell 里的赋值往外带值。
- **qemu/run.sh 的 `$QEMU_LOG`**：只在 `$( )` 里的函数内赋值，父进程六处引用
  在 `set -u` 下是 unbound variable，**五处失败分支在打印 howto 之前就被带走**。
  改成工作目录由调用方持有。这正是 command-safety 自己写下的那条坑。
- **gate skill 的「证明它会红」示例实测退出码 0**：`>>` 追加落在「## 历史版本」
  之后，而正文扫描在那里停机。换成另起目录的样本。
- **规范本体只定义一次**：`version-discipline.sh` 的 `GOVERNED` 为唯一权威，
  CLAUDE.md / README / session-wrapup 三处各自的清单改成链过去；
  README.md 与 I18N 纳入治理（此前 README 载着规范性内容，doc-lint 扫得到它，
  但版本纪律与清单对账都够不着）。
- 去重与陈旧：CLAUDE.md 与 README 重复且已漂移的多语言段（「不必读英文」/
  「不必学英文」）合并到 README；README 三节讲同一件事合成一节；
  install.sh 的「三件事」实为六样（含写到项目根的 `litmus/`）；
  gate skill 的阶段表 4 行补到 9 行、假失败表里那句不存在的消息换掉；
  「三语」写死的 15 处改成与 `I18N` 的 `languages=` 一致的说法。
- gate.sh：「未检查副本是否落后上游」进入汇总的「本次未跑」——此前只在开头
  warn 一行，末尾却报「N 个阶段全部通过」。

**本轮复核后的第二批**（两个审核 agent 各查一遍，逐条现查过才收）：

- **en 版 `show-me-test.md` 丢了一个否定**，「不保证任何人达到了正确」被译成
  「保证没人能达到正确」——本仓最核心的那条分界句，意思正好反过来。
- **gate-lint 与 shell-lint 只扫 `scripts/`，扫不到仓根**：`install.sh` 在
  `GOVERNED` 里、是使用者跑的第一个脚本，它那 4 处光秃秃的 `die` 一处没查到。
  扫描范围改成整个包，样本排除写成相对本次 SCAN 的前缀（写死 `*/fixtures/*`
  会让样本把自己排除掉——9 个用例盯着这一点）。
- `sop-first.md` 的窗口写成「从 bad 那行起 4 行内」差了一格，脚本是 5（含 bad 自己）。
- 「规则清单」在消费项目里也跑（比对的是装进去的那份副本），gate skill 说反了。
- `machine-first.md` 对 lkmm 对照组的描述停在「全局数一条 Sometimes」的旧口径。
- 「分工判据」原本写在三处且已开始各说各的，正文留 CLAUDE.md，README 链过去；
  `design-doc-discipline` 与 `writing-economy` 逐字重复的两行删掉一处。
- README「里面有什么」表漏了 `rules/`，而两行之后就说规范正文是 `CLAUDE.md` + `rules/`；
  安装段与 en/ja 说的不是一回事（它们用安装器仓，zh 只写了手动路径）。
- en/ja 的 `CLAUDE.md` 外链到 README 的节名在译本里不存在；两仓 CHANGELOG 的
  0.0.23 还写 51 例。
- `skills/` 与 `templates/` 逐字节复制进译本仓、内容是中文，与「用日文的不必读中文」
  冲突。**没修，按 GLOSSARY 的办法把缺口显式写在文件里**——补法是让它们进 MANIFEST
  逐篇翻译，那是设计判断。

**第三批：126 个变异跑出来的判别力盲区**（用例 69 → 111）

- **`gate.sh` 自己此前零覆盖**，而它是整道门禁唯一的判决点：把 `run_stage` 改成
  无条件记 PASS，造一处真实文档违规，**退出码是 0**、自检全绿。补法是把 `gate.sh`
  连同 `lib.sh` 拷进临时包、子脚本换成桩——测的是记录与汇总的判读，不递归。
  现在钉住「全绿 ⇒ 退出码 0 且阶段清单齐全」与「任一阶段红 ⇒ 门禁红」（7 个阶段各一条）。
- **`GOVERNED` 9 项只有 2 项有人盯**：其余 7 项可静默摘掉，`VERSION` 的整行匹配
  写成 `grep -q` 后新增一个 `VERSIONING.md` 就能冒充抬过版本。逐项补样本。
- **`i18n-sync` 8 条检查只钉住 1 条**，其中三条在脚本注释里被标成「实测过的回归」——
  清单陈旧、GLOSSARY 漂移、抄了清单没重译，都可以再犯一次。8 条分支各补一例。
- **`lkmm` 的配对判据可以被静默改回旧 bug**（全局数一条 Sometimes 就算），
  补「3 条无关 Never + 1 条无关 Sometimes」的样本。
- **`check.sh` 零用例**、**`lib.sh` 的 gawk 与 LC_ALL 守卫零用例**、
  **`manifest` 的 `CLAUDE.md` 覆盖零用例**、`show-me-test` 的未跟踪文件扫描零用例，
  各补齐。摘掉 LC_ALL 守卫后 C locale 下编号检查会把每处正确引用都误判成
  「括注没闭合」——这类「悄悄改判」比漏检更难发现。
- **一个样本触发多类违规、却只有一条 `want`**：`原先`/`以前`/`之前` 三条模式共用
  一句 why，删任一条都不红——把 why 拆开，一条一句。`pkill -f` 与 `killall`
  拆成两个样本。
- **豁免也要有人盯**（方向相反的盲区：门禁开始误拒，人就会绕过它）：注释里的 `bad`、
  bad 与 howto 之间的注释、历史节里按规则写的「曾经 X / 现在 Y」、heredoc 里的赋值、
  与外层同名的 `local` 变量、在 `$( )` 里设了外面不读的全局——各配一个踩在边界上的绿样本。
- 复跑那 126 个变异：**门禁察觉不到的从 55 降到 6**，其中 5 个验过是等价变异
  （按 `test-discipline.md` 分开记，不补样本），剩下 1 个补掉——`lib.sh` 里 `die`
  少第二个参数时的运行期兜底。gate-lint 在调用点静态拦裸 `die`，但它自己写明有几种
  形态「认不出、不判」，那几种只剩这道兜底，而兜底此前没人盯。用例 111 → 112。
- 规则增补：sop-first 的出路要求覆盖 `die`；design-doc-discipline 给检查代码的
  坑注释开例外；kb-discipline 写明 `INDEX.md` 不要历史节；
  command-safety 标出哪两条已经是检查。

## 0.0.23 — 2026-09-02

对抗审计后的整批门禁修复（审计报告见当轮会话记录）：

- doc-lint：围栏未闭合判红；「历史版本」之后再开 `##` 小节判红；不变量定义
  不再认历史节里的表格；rules 文件留历史节判红；rule-definition 标记限定
  CLAUDE.md / rules / skills 三处；not-numbers 豁免已登记编号判红；
  「脚本文件」「同上游」这类构词不再误判为指代（首字排除）。
- gate-lint：bad 认全形态（单双引号、变量、`;{|&`/`then`/`else` 之后）；
  注释里的 howto 字样不算出路；汇总豁免收紧为「失败/未通过/未过：$计数」。
- Show me test 拆成 show-me-test.sh：注释里的 `#[test]` 不算；tests/ 只认 `.rs`；
  build.rs 也是代码；在默认分支上基准退到 HEAD~1，先提交再跑门禁不再「无对象可判」。
- lkmm：每条 Never 必须有同名 `-nofence` 配对对照，全局数一条 Sometimes 不再算数；
  静态检查前置到 herd7 探测之前；全部拒绝补 howto。
- i18n-sync / manifest：失败分支的 diff 诊断管道在 set -e + pipefail 下会把脚本
  中途带走（howto 与后续语言全丢）——补 `|| true`；三语同步前先验清单新鲜度；
  GLOSSARY.md 并入共享区同步。
- 新增 version-discipline.sh：改规范本体不抬 VERSION 直接红（原是提醒句）。
- selftest 泛化到全部门禁脚本：doc-lint / gate-lint / lkmm 样本目录 +
  show-me-test / version-discipline / manifest / i18n-sync 脚本化用例，共 54 例。

## 0.0.22 — 2026-09-02

- session-wrapup 增补「同一个仓里有没有别的会话在飞」一节；show-me-test 增补
  变异清单；test-discipline 增补「确定性模型 N 轮」「变异测试证明的是断言会红」；
  GLOSSARY 新增变异测试相关术语。

## 0.0.21 及更早

见 `git log --oneline`——每条提交信息都按「版本：改了什么」写。
