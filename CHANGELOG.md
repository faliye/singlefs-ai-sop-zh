# CHANGELOG

规则与门禁的版本历史。`CLAUDE.md` 与 `rules/*.md` 不留历史节（design-doc-discipline），
历史一律记在这里；逐条改动细节见 `git log`，提交信息即变更说明。

## 0.0.51 — 2026-09-19

**kb 的指代检查补上光秃的方位形态；2026-09-17、09-18 在 singlefs 上的几条实测教训写进规则；现状句一律不带日期。**

门禁（`scripts/`，三仓同一份）：
- doc-lint 的上下文指代补上不带「见」的光秃形态（「上述」「下文」「下表」「以下是」「逐条如下：」「上面三条」等）。
  判据是在 singlefs 的 357 份 kb 上量出来的：旧判据之外新命中 103 处，逐条看过全是真指代；
  「上面 / 下面」只认指文档位置的形态，「前面 / 后面」「上方 / 下方」不查。样本 dirref（红）、dirrefok（绿）。
- 上下文指代与自称两条判据先单独编一次 grep，编不过当场红：那条 grep 外面套着 `|| true`，编不过（退 2）与「没命中」长得一样，
  写圈码区间时整条检查对每份 kb 静默判绿。selftest 新例「判据编不过要当场红」，用例 320 → 323。
- 消费侧：用这一版的 doc-lint 扫 singlefs，有两处真指代会红（`.claude/kb/experiments/154-…` 第 8 行「下文 H5…」、
  `.claude/kb/milestone/02-second-txn.md` 第 266 行「下面的数」「下面引的六家」），由 singlefs 改。

规则：
- kb-discipline：第 1 条补上光秃形态与「上面 / 下面」的门禁射程；第 8 条下新增「带日期的现状快照也是历史，会停在那一天」：
  现状句不带日期，已有的删掉日期、保留事实。第 2 条随之改成「日期只跟着事件走」，
  verify-before-claiming「落实」里「留着旧的至少还带着日期」一句随之改写——三处原来互相冲突，按用户定的「现状句坚持不带日期」调和。
- evidence-discipline：「独立计数只对上了总数，印证不了拆法」；「回扫要按旧说法搜，不能只按新名字搜」。
- test-discipline：新节「分段计时在被测进程里计，不在转发输出的循环里计」「实验要写清为哪个决定而量、量到哪算够」；
  「阳性对照的答案，不能放在被测的那一方读得到的地方」；「门禁管哪一半」的全靠人清单补上这三条。
- show-me-test：「射程只到 `.claude/gate.d/`」，项目别处的研究脚本与 hook 要另接本地阶段，交给 gate-lint 与 shell-lint 两个 lint
  （singlefs 的研究脚本：gate-lint 84 处、shell-lint 18 处）；推论第 5 条「交回的清单能被机器核全，不等于判对了」。
- command-safety：新节「起了后台任务和子 agent，就要定时复检，不强制结束」。只报告不处置的是检出卡住与空转的工具，
  拒绝某一种危险写法的钩子不在此列（原句读宽了与 session-wrapup 第 4 条相冲）。
- session-wrapup：「不写进会话私有的记忆」。

## 0.0.50 — 2026-09-16

**一轮全面审核：`--staged` 这条路上三个真 bug、doc-lint 对规则本体整篇免检、一批 0.0.48 之后的残留，一起修掉。**

门禁（`scripts/`，三仓同一份）：
- `gate.sh --staged` 的握手变量 `GATE_STAGED_FROM` 漏进里层起的子进程：selftest 嵌套跑的 gate.sh 拿它当自己的项目根去找兄弟目录，
  「上游比副本旧」「副本落后上游」两例反红——singlefs 在 0.0.49 上跑 `--staged` 必红。用户指定的 `GATE_BASE` 同样漏进 selftest，
  show-me-test 两例退 3。现在 gate.sh 读完 `GATE_STAGED_FROM` 就 `unset`，selftest 起子进程一律 `env -u` 掉这两样。
- `--staged` 在 SOP 仓自身上跑不了：里层用源仓的脚本、ROOT 却是临时树，被当成消费项目报「缺版本戳」判红，
  只在 SOP 仓跑的三个阶段还静默不跑。现在源仓就是本包时跑临时树里那份 gate.sh，i18n-sync 从源仓的父目录找兄弟语言仓。
- `--staged` 的 diff 基准比直接跑窄一档：临时树是 detached HEAD，`@{upstream}` 解析不到，落到 HEAD~1；
  「分两次提交就绕过去」那条口子在这边开着（实测：直接跑 origin/master，`--staged` HEAD~1）。现在基准在源仓上算好再传进去。
- shell-lint 的 S3 命中 `pgrep -f` 后还要同一行含 `xargs|kill|until|while|if` 子串才红：`pids=$(pgrep -f X)` 下一行 `kill $pids`、
  `while` 与 `pgrep -f` 分两行写都全绿，`pgrep -f notify` 却因含子串 if 被判成等待循环。现在命令位置上出现就红，配样本 `pgrepsplit`。
- 「缺版本戳」记 FAIL 却用 `warn` 打、没有出路，gate-lint 只认 bad / die / ✗，看不见它。改成 bad + howto。
- doc-lint 的 `rule-definition` 牌此前整篇免检：往 rules/ 里塞翻译腔、「原先是 X」，doc-lint 全绿，同一句放进 README 当场红。
  现在正文照查，只把反引号与「」里举的例子在比对词表前挖掉；14 篇规则和 CLAUDE.md 从「跳过」变成「检查」（检查 14 → 29）。
  样本 rulesprose / ruleshiststate（红）、rulesexample（绿）。
- doc-lint 排除「装进项目的副本」用的是通配 `*/singlefs-ai-sop/*` 加「ROOT 在包里就不排除」的例外，扫描范围随包所在的路径变：
  同一个样本 projok 在 zh 仓里「检查 1」、在 singlefs 的副本里「检查 2」，同步过去 selftest 当场红一例。
  改成相对 ROOT 的 `$ROOT/.claude/<family>/*`；新用例把脚本拷到路径里带 `/singlefs-ai-sop/` 的地方再跑同一个样本。
- **`--staged` 经项目包装脚本调用时被静默吃掉**：包装把项目根放 `$1`、用户参数接在后面，而 gate.sh 写死「`$1 == --staged`」，
  于是规则与 skill 里推荐的 `bash .claude/scripts/gate.sh --staged` 根本没进那个分支，门禁照常跑工作区、一声不响。
  现在参数顺序不限，认不出的参数直接拒绝。
- **heredoc 识别把三种不是 heredoc 的写法当成开头**（`<<<` here-string、注释里的 `# … <<PY`、`$((1<<SHIFT))`），
  其后整个文件不再检查——越大的脚本越容易中。定界符判据收进 `lib.sh` 的 `HEREDOC_RE` 一处（同 `CMD_POS` 的先例），注释行在判之前跳过。
- **`… | grep -q` 在管道末端**：grep 找到就退出，上游拿 SIGPIPE，`pipefail` 下整条管道退 141、判据当假。
  文件大过管道缓冲（64 KiB）就静默不判了（实测：同一份没报计数的脚本 4 行判红、338 KB 变绿；带测试的 108 KB 新文件被判没带测试）。
  gate-lint 的 G3、show-me-test 的两处、doc-lint 的编号定义比对都改成 `grep -c`。
- **gate-ok 记的是跑完时的 HEAD**：另一个会话在这一轮跑的过程中提交，那个从没验过的提交被一并盖章，此后永远落在 diff 窗口外。
  改成记开跑时的 HEAD；`GATE_BASE` 被显式指定（`--staged` 的里层也是）时不记。
- **diff 基准取 `@{upstream}` 的 tip 而不是 merge-base**：fetch 了没 merge 时，上游 tip 上有本地没有的提交，
  于是工作区干净也被判「改了 crates 代码但没有任何测试改动」。假红会把人逼去绕门禁。
- **`GATE_BASE` 不校验**：写错一个字母（`orgin/master`），`changed_files` 按「空仓」处理、只看未跟踪文件，
  show-me-test 报「无对象可判」、门禁绿、gate-ok 还往前推。现在解析不到就拒绝并给出路。
- **判「被门禁的是不是 SOP 仓自身」比的是 `pwd`**：经符号链接跑就判成消费项目，报「缺版本戳」判红，
  只在 SOP 仓跑的三个阶段还静默不跑。改成 `pwd -P`。
- **项目里单跑 gate-lint / shell-lint 会报副本里的样本**：副本带着一整套故意写坏的样本，被当成项目自己的违规。
  两个 lint 都排除 `$ROOT/.claude/<family>/`（与 doc-lint 同一形态）。
- **kb 的「## 历史版本」标题行尾多一个空格，整条判据落空**：正文扫描不在历史节停机，还报「必须有历史节收尾」。改成去掉行尾空白再比。
- **`x="$(sed -n … I18N)"` 在 I18N 不在时退 2**，`set -e` 当场把脚本带走、零输出（gate.sh、gate-lint、shell-lint、doc-lint 两处、push-all 共六处；第四轮又查出两处）。
- **pre-push 不看要推的是哪个 ref**：推特性分支、tag，甚至 `git push --dry-run`，都会把另外两个语言仓**真的**推出去。
  现在只有推 master 才触发；`CLAUDE.md` 写明了它的射程与 `--dry-run` 这个拦不住的坑。
- **install.sh 铺下去的文件是 0600**（`mktemp` 的权限被 `mv` 原样保留），改成 644。
- `env.sh` 补上门禁自己依赖却一项都没查的工具：gawk、sha256sum、timeout，以及 git ≥ 2.28、bash ≥ 4.3、`sort -V`。
  `manifest.sh` 的排序钉成 `LC_ALL=C`；selftest 隔离用户的全局 / 系统 git 配置。

第三轮（前两轮报告里剩下的项，一并并入）：
- **shell-lint 的 S5 只认一种拼法**：`rm -rf "$d"/x`、`rm -rf -- "$d/x"`、一行里第二个参数没守卫，此前全绿。改成两步判：先挑出 `rm -r`，再逐个参数找没守卫的变量路径。
- **从 git 钩子里跑 `--staged` 会失败**：钩子里 git 设的 GIT_DIR 是相对路径，gate.sh 换了工作目录之后它指错地方，worktree 建不起来，而出路写的是「先跑 git worktree prune」。开头清掉这一组环境变量；建不起来时把 git 的原话打出来，出路改成 `worktree remove --force`。
- **`--staged` 的清理只留 EXIT 一处**：中途任何一条路径退出都带走临时 worktree。
- **「规范版本不一致」那条拒绝一出口就是 unbound variable**：族名定义在几十行之后。挪到用它之前；它的出路也不再叫人在没有 .git 的副本上跑 git log。
- **本地阶段各按各的口径取 diff 基准**：gate.sh 算一次，以 `GATE_DIFF_BASE` 导给每个阶段。
- **「无对象可判」与「判过了」分开**：naming-lint 没有 .rs 时退 3、记「本次未跑」；doc-lint 加 `--not-impl`，没有词表的语言那几条检查进汇总的未实现清单，不再记 PASS。
- **gate-lint 也扫 .py**，只跑「直接打印的 ✗ 要有出路」那一条（singlefs 的 lib-*.py 里 13 处此前一处没查）。
- **doc-lint 两条「已废弃」模式共用一句消息**，删掉任一条样本照绿；拆开，各配样本。
- **install.sh 会把版本戳往下写**：副本比戳旧时照写，降级随提交传给所有人。现在拒绝。包装脚本在副本不在时说清怎么办，不再只有 bash 的「No such file」。
- **i18n-sync 三处只认 `.git` 目录**，在 worktree 上做译文同步会被拒。这一版的发布就是在 worktree 上做的，三处都实测过。
- **日期不许是编的**：三份样本与一处 skill 示例写着 2026-01-01，比本仓第一个提交早大半年，门禁一直是绿的。全部改成真实日期：样本用它自己的创建日，skill 示例改用 `date +%F`。doc-lint 查历史条目与「实测（日期）」，changelog-lint 查版本节日期，selftest 查样本里的日期。下界是被查项目自己的第一个提交往前宽限 7 天（只在它本身是 git 仓顶层、又不是浅克隆时问 git，不往上找外层仓），拿不到就用本包的起点 2026-08-26；上界是最晚的时区（UTC+14）的今天。两端为什么这么定，见第四轮。
- gate.sh 的判决分支补用例（本地阶段交给两个 lint、Show me test 退 3、跑的不是副本、有 .rs 没 Cargo.toml、`--staged` 拷副本），此前把它们改成记 PASS，自检一例都不红；bump.sh 与 i18n-sync `--stamp` 的拒绝分支此前零覆盖，补上。
- lib.sh 删掉没人调用的 `added_lines`。

第四轮（三个模型分正推、反推、校验审核这一版，审了两轮，查出的问题一并修掉）：
- **日期检查把真日期判成不可能**。上界取本机时钟的日期，而本机时钟是 UTC：东京 00:00–09:00 写下的当天日期被判「晚于今天」，
  singlefs 的 194 个提交里有 8 个在这个时段写下了晚于提交时 UTC 日期的历史条目。下界卡死在第一个提交，而 git init 之前做的工作会带着当时的日期进第一个提交：
  singlefs 的初版提交里就有 5 条前一天的历史条目，按移交前的 0.0.50 跑，它的门禁在这 5 处是红的（审核实测）。
  现在上界按最晚的时区算，下界往前宽限 7 天（实测只早一天），2026-01-01 这种照样拦得住。4 个用例钉住两端，时钟用假的 `date` 钉死。
- **点名退役包装的机制随 QEMU 一起删掉了**，而同一版就出现了新的退役包装：singlefs 的 `.claude/scripts/lkmm.sh` 转发到已删的脚本，
  install.sh 照常刷戳，跑它得到的出路是「副本没装」。机制放回来，改成通用的，见下面移交那一节。
- **未实现清单不再提准入标准**：两个 QEMU 键删掉之后，项目一个最终判据的阶段都没接，汇总里也一字不提。换成不点名装置的「最终判据」，见下。
- **I18N 不在时零输出还剩两处**：doc-lint 读 `this=`、manifest 读 `this=` 与 `reference=`。补上 `|| true`，各配一例。
- **selftest 搭译本样本时把盖章的输出丢进 `/dev/null`**：盖章与回读的形态一对不上，lib.sh 的 `set -e` 把整个自检在第 245 例带走，
  后面 58 例一个没跑，只剩退出码 1。现在失败时打出原话再停。
- **误删了与虚机无关的内容，放回去**：`command-safety.md` 的「测试镜像一律放临时目录」「破坏性操作先看清楚再动」两节，以及表里的 `mkfs`、`dd`、`lsblk`。
  崩溃点重放一样要镜像、要 mkfs，singlefs 的代码与脚本也在按标题引「测试镜像一律放临时目录」。`env.sh` 的 dmsetup 查的是崩溃点重放的块层写记录，也放回去。
- 文字：`show-me-test.md` 收进来的「跳过清单」一段写成了 gate-lint 已经在查 `noskip`，实际没有这条检查，改成写明还没做成；
  doc-lint 文件头注释还说 rule-definition 整篇跳过；pre-push 注释说 `--dry-run` 的坑写在 README，实际在 `CLAUDE.md`；
  `test-discipline.md` 两处「不可推翻」上一轮只改了一处；日期那条写的「五份样本」实为三份，fence 样本里写着 2026-01-01 的那一处改成它的创建日；
  env.sh 那句「sort 不认 -V」的例子改成可复核的写法（0.0.9 → 0.0.10 会被判成降级，审核实测）。
- 第二轮审核接着查出：
  - **机器上的 date 不认 `-d` 时，日期下界静默消失**，2026-01-01 照样放行：日期检查是在 `if` 条件里调的，`set -e` 不管。
    doc-lint 与 changelog-lint 开头先试一次 `date -d`，不行就停；env.sh 也查这一项。
  - **浅克隆里「第一个提交」是截断处**，拿它当下界，项目越老误判越多。浅克隆时不问 git，退到本包的起点。
  - **en / ja 仓的「Measured (日期」「実測（日期」一条都没查**：只认中文的「实测（」，而没查的这一半也没列进未实现清单。三种写法都认了。
  - **退役包装只认现行形态**：0.0.49 以前铺的三行包装认不出，照常刷戳。两种形态都认；项目照模板给还在的共享脚本加的包装不算退役。
  - **宽限 30 天放过的编造日期太多**：实测只早一天，收到 7 天。
  - **selftest 没有一例管「工作区指纹不碰真索引」**：把指纹改成直接在真索引上 `git add -A`，算出来的树逐字节相同，原有用例一例不红，
    而使用者每跑一次门禁，没暂存的改动就被悄悄暂存。补一例，直接比调用前后的 `git status`。
  - 文字：并进来的「连词写成连词」一节要求不用 `⇒`，而这一版新写的规则行里就有 7 处 `⇒`，改成连词，并写明门禁不查、旧文还没回扫；
    `show-me-test.md` 的「跳过清单」那句不再写一个没实现的豁免语法；「最终判据」写明先把准入标准写进项目 kb，只做到一部分的阶段不写这个键；
    三处把模型对拍也说成要录制流的句子改正；`session-wrapup.md` 那条实测写明撞的是不带路径提交那一种；
    gate skill 的阶段表补上「工作区跑的过程中没变」；gate.sh 叠在一起的两个节标题分开；doc-lint 与 changelog-lint 几处注释跟上宽限。
- 会红的证据：日期上界、宽限、不往上找外层仓、退役包装、照模板包还在的共享脚本不算退役、两处 I18N、最终判据，第一轮审出的这 8 处修复，
  加上第二轮的 `date -d`、浅克隆、英日实测写法、旧形态包装、宽限 7 天、指纹不碰真索引 6 处，逐一改回旧写法，每一处都让针对它的用例变红。
  盖章失败时打原话那一处没有专门的用例：是把盖章回读改坏，看自检停在第一次搭样本处、打出原话来验的。

另一个会话同一时段的改动，一并收进这一版：
- **门禁跑的过程中工作区变了要判红**：gate.sh 开跑与收尾各记一次工作区指纹（跟踪的文件加没被忽略的未跟踪文件，用拷出来的临时索引算，不碰真索引），
  对不上就判红，出路是等改动停下再跑或者用 `--staged`。selftest 两例：中途改文件要红，中途只暂存不算变；
  「gate-ok 记开跑时的 HEAD」那一例改成跑之前就写好内容，跑的过程中只让 HEAD 动。
- `show-me-test.md` 两段：报了查了多少还不够，没查的也要逐个列出、清单现算；成功行里报的数也要有样本钉住。
- `evidence-discipline.md` 两段：进结论的每个数先问是量的还是拍的；撤回时扫出来的清单不能一把全替换，先分清说的是现状还是那一次的事。
- `writing-discipline.md` 一节：连词写成连词，名词串拆回句子。
- `session-wrapup.md` 两条：不带 `--staged` 跑时工作区变了会判红；「只提交这些路径」不等于 `git commit -- <路径>`。

selftest 268 → 320 例：三轮修复补了 58 例，第四轮补了 12 例，另一个会话的改动带进 2 例，移交 QEMU 与 herd7 删掉 20 例（见下）。会红的证据：三轮的修复逐一改回旧写法，每一处都让针对它的用例变红。
其中四条用例第一版是假的，变异之后一声不吭：大文件样本的填充行写成了注释、`GIT_DIR` 写成了绝对路径、die 之前另外手工清了一次、本地阶段自己判红盖住了 lint 的判定。另有两次变异把 case 语句改坏了语法，重做成整行替换才算数。都改到会红为止。

规则与文档（各语言一起改）：
- `command-safety.md`：会红的检查是五条不是四条，补上 `pgrep -f`；S3 的描述改成与脚本一致（命令位置上一律红）；一处「基于」。
- `sop-first.md`：拒绝形态「两种」→ 三种；「17 处 die 全都免检」「此前全部免检」两句历史叙述删掉或改成实测记录。
- `show-me-test.md`：本地阶段那句「此前它不在任何 lint 的扫描里」改成实测记录。
- `test-discipline.md`：两处「不可推翻」改成与节标题一致的「不可证伪」；「本节主结论」改成点名。
- `verify-before-claiming.md`：节标题「它定了没，和它到底怎么定义的，是两个问题」去掉内层引号，与两处引用一致。
- `kb-discipline.md`：写明「本文件」门禁不查，靠人。`design-doc-discipline.md`：doc-lint 只扫 `.md`，代码注释那一条靠 review。
- `engineering-philosophy.md`：howto 要求在 `sop-first.md` 不在 `show-me-test.md`；判据句改成引用 `machine-first.md` 的原话。
  `machine-first.md`：与 `engineering-philosophy.md` 逐字重复的那段缩成一句加引用。
- `code-discipline.md`：三处处置标签（「废弃这条理由」「留下，而且更硬」「交给类型」）归到声明的六种里。
- `writing-discipline.md`：门禁管哪一半补上规则本体也查、只豁免举例。
- skills：`gate` 补 `--staged` 用法、门禁自检与 shell 纪律的失败原因列全、en / ja 副本下规则清单不适用、rule-definition 新语义；
  `crash-test` 的 gate-covers 键补上「命名纪律（shell）」；`decide` 的「本工程」→「本项目」。
- templates：kb 三篇指向规则的相对路径改成从 `.claude/kb/` 能解析的 `../singlefs-ai-sop/rules/…`；项目模板的 shell-lint 注释列全。
- README：「克隆」→「副本」；三关那段「由 gate-lint 强制」与「靠人判断」两句打架，改成门禁只管第 3 关的形式。
- en / ja：顺带修 0.0.48 / 0.0.49 译文里的用词不一（英文 remedy 同时译「出路」「改法」，日文「写死的 pid」「虚机」各两种写法）。
- 第三轮：`test-discipline.md`、`verify-before-claiming.md` 补「门禁管哪一半」，写明哪些做成了检查、哪些全靠人；`command-safety.md` 补「进程边界上的三种静默失效」；`code-discipline.md` 写明 shell 命名纪律对现有脚本还没回扫过；`sop-first.md` 去掉可数声明；英日两仓「门禁管哪一半」各有两种译法，统一成一种。

QEMU 与 herd7 整个交给 singlefs。这两样只有 singlefs 在用，怎么测、怎么验、接不接进门禁都由它自己定，本包不再测也不再验：
- 删掉 `scripts/lkmm.sh`、`scripts/fetch-deps.sh`、`scripts/fixtures/lkmm/`、`templates/litmus/`。门禁没有 LKMM 阶段了。
- 未实现清单去掉「QEMU 真实负载」「QEMU 崩溃注入」两个键，换成不点名装置的「最终判据」：准入标准由项目定，
  项目的阶段做到了、写了 `# gate-covers: 最终判据`、这一轮跑过且通过，这一项才换成「由谁覆盖」。
- `install.sh` 不再铺 `.claude/scripts/lkmm.sh`，不再往项目根播 `litmus/`。点名退役包装改成通用的：`.claude/scripts/` 里与包装模板（现行或 0.0.49 以前的形态）一字不差、
  它转发的共享脚本这一版里又没有的，一律点名并挡住版本戳，不再只认写死的 `qemu.sh`。包装全文只写在 `wrapper_text` 一处，铺新包装与认退役包装比的是同一份文本。
- `env.sh` 不再查 qemu-system-x86_64、fio 与 `/dev/kvm`。
- `i18n-sync.sh` 去掉 `.litmus` 的溯源标记形态，`install.sh` 剥标记时也不再认它；`manifest.sh` 的译文清单与覆盖率检查不再收 `.litmus`；
  `show-me-test.sh` 的出路删掉并发与内存序那两行。
- 规则：`show-me-test.md` 的「最终判据是 QEMU/KVM 压测」改成「最终判据由项目定」。`machine-first.md` 前提二不再点名 herd7 / LKMM，
  写明工具、判别力与绑定代码由项目自己定，依据指向 singlefs 的 kb；表里两种「模型」分开写，被穷举的那个叫「形式模型」。
  `command-safety.md` 删掉「QEMU 虚机必须把 pid 写进文件」一节；等进程结束那句的「虚机之类」改成不是自己起的进程；
  结果抓取那句的「BIOS 转义序列、内核日志、串口噪声」改成不点名的写法；子 shell 赋值的实测记录不再点名 QEMU harness。
  `test-discipline.md`、`code-discipline.md`、`sop-first.md` 跟着改。
- skills：`crash-test` 删掉 LKMM、QEMU 两节，gate-covers 的键是模型对拍、崩溃点重放、最终判据、命名纪律（shell）；`gate` 删掉 LKMM 那一行。
  项目模板、README 跟着改；GLOSSARY 的「对照组」原先按 litmus 定义，改成与 `test-discipline.md` 一致的属概念：阳性对照或真实基线。
- selftest 删掉 lkmm 样本与 litmus 溯源标记两组，共 20 例；退役包装那 3 例改成拿 lkmm.sh 造。shell-lint 样本里 `stop_vm`、`HAS_KVM`、`KERNEL_PATH` 这类名字换成中性的。
- 删掉之前的原文放在 singlefs 的 `.claude/handover/qemu-herd7/`。
- **这一版不抬版本号，门禁不会提示规矩变了**：singlefs 的版本戳本来就是 0.0.50，「规范版本」「副本与上游同版本」两个阶段照样绿，同步全靠人。
  同步时 install.sh 会拦下两处：`.claude/install-owned` 里两行 litmus（本包不铺这两个路径了）、`.claude/scripts/lkmm.sh` 包装（转发目标已删）；
  前一处先拦，改完再跑才看得到后一处。门禁会拦下一处：`gate.d/55` 头部的 `# gate-covers: QEMU 真实负载`，这个键不在清单里了。
  按 55 号头部写的，它跑真实负载加两个必须判红的对照，没有崩溃注入，所以这一行该删掉；准入标准写进 kb、阶段做全了，再写 `最终判据`。
  `litmus/` 从此没有共享阶段判，要继续判，就把移交过去的 `lkmm.sh` 接成项目自己的阶段。其余不会红、但同步后说的不再是真话的地方，列在移交目录的 README 里。

没动的：`evidence-discipline.md` 里「查出五处……」括号只数得出四处，原始记录在 singlefs，这里核不了；GLOSSARY 里七个全仓未用的术语，待核；
changelog-lint 与 doc-lint 的样本里还有一批日期早于样本入库那天（例如 2026-09-10 入库的样本写 2026-09-01），都在上下界之内。
它们是样本里虚构的版本节与历史条目，不是 2026-01-01 那种占位日期，这一版没改，改不改待定。

另外两项有意没做。`evidence-discipline.md` 的「门禁管哪一半」没补：这一版收进了另一个会话对那一篇的两段改动，补这一节要再走一轮审核。规则里的案例段瘦身与重复判据归并没做：要删的是证据，拿不准就留着。

## 0.0.49 — 2026-09-14

**0.0.48 收尾时查出的四处门禁缺口，一起修掉。**

- `gate.sh --staged` 判红时漏下临时 worktree：外层写成「里层; rc=$?」，而 lib.sh 开着 `set -e`，里层一红外层就当场退出，
  清理走不到——判红正是最要看结果的时候。实测：新仓里里层通过时 worktree 登记 1 → 1 个，判红时 1 → 2 个。现在退出码在 `if` 里取。
- `project_root` 只认 `.git` 目录，而 git worktree 里的 `.git` 是文件；九个脚本都写成 `${1:-$(project_root)}`，
  于是在 worktree 里不带参数跑，一路找到 `/`，在 `set -e` 下零输出退 1。现在 `.git` 是文件也认，真找不到时报出来并给出路。
- 「副本与上游同版本」在副本比上游新时也报「副本落后上游」，出路给反了（去重拷副本，拷回来的是旧版）。
  现在分两个方向：上游旧就报「上游比副本旧」，出路是更新上游。
- `install.sh` 会把项目删掉的模板重新铺回来（`put` 只管不覆盖已有的文件）。现在接管清单里登记了、项目又删掉的那份不再重铺。

selftest 267 例（+7：--staged 判红清理、worktree 里找项目根、找不到项目根要说、上游旧 / 副本落后两个方向、
接管后删掉的不重铺及其回读）。五处修复逐一改回旧写法，每一处都红在针对它的用例上。

另一个会话留在工作区、一直没提交的两条实测教训，一并收进这一版（它们原本没抬版本、没刷清单、没翻译）：
- `command-safety.md`：等待循环里的 `pgrep -f` 会命中循环自己所在的命令行，永远不退出；等进程结束用写死的 pid（`kill -0`）或 `wait`。
  `shell-lint.sh` 的 S3 跟着扩到 `if` / `while` / `until` / `!` 之后的命令位置（`lib.sh` 的 CMD_POS 是这一处的唯一定义），配样本 `pgrepwait`。
- `evidence-discipline.md`「判据自己也会写错」加第四种形态：跑前条款给的改法碰不到打中的格；打中之后先问的三句变四句。

selftest 因此再加 1 例，共 268 例。

## 0.0.48 — 2026-09-14

**singlefs 的 `crates/` 有了代码，LKMM 与 QEMU 两道门禁跟着改。**

LKMM（`scripts/lkmm.sh`）：
- **对照组按内容认**，不再只按文件名：去掉注释与首行之后，对照组只许比 Never 那条少几行屏障，
  或把 `smp_store_release` / `smp_load_acquire` 放宽成 `WRITE_ONCE` / `READ_ONCE`，至少一处。
  只按文件名认时，换一个 exists、换一个读者的「对照组」照样过闸；名字前缀撞车（`a` 与 `a-b`）时，`a-b` 的对照还会被算成 `a` 的。
- **每条 Never 要绑到代码**：头部写 `singlefs-models: <路径>::<函数名>`，门禁查文件在、`fn` 在、`crates/` 下有 `.rs` 写出这个 litmus 的文件名；
  不对应代码的写 `none —— <理由>`。herd7 只判 litmus 写下的形态，代码改了顺序而 litmus 没跟，判定照样是 Never。模板 `commit-publish.litmus` 写成 `none`。
- 新增 `--static-only`：只跑不需要 herd7 的检查，全过也退 3。selftest 用它喂样本，样本的判定不再随机器装没装 herd7 变。

QEMU：
- **删掉共享的 `scripts/qemu/run.sh` 与门禁阶段「QEMU harness 自检」（`GATE_QEMU`）。** 它不挂盘、只收 shell 脚本、不抓结果，
  singlefs 为此另写了虚机装置，门禁 55 用的是那一份——共享那份的自检证明的是一个没人用的东西。
  虚机装置归项目，装置要守的规矩仍在 `rules/command-safety.md`。
- `install.sh` 不再铺 `.claude/scripts/qemu.sh`；项目里一字未改的旧包装会被点名，删掉之前不刷版本戳。

`gate.sh`：
- **未实现清单由项目声明覆盖**：本地阶段头部写 `# gate-covers: <项>`，这一轮跑了且通过，那一项才换成「由哪个阶段覆盖」；
  写了清单里没有的项判红。清单拆出「QEMU 崩溃注入」（准入的最终判据），与「QEMU 真实负载」分开。
  此前清单写死：singlefs 每次门禁都跑崩溃点重放与真设备阶段，汇总仍打印「缺被测对象」「崩溃一致性尚未纳入门禁」。
- 本地阶段退出码 77 记「本次未跑」，不记通过、不算覆盖。

规则跟着改：`show-me-test.md`（虚机装置归项目、覆盖声明、退出码 77）、`machine-first.md`（对照组按内容认、litmus 要绑代码）；
skill `crash-test`、`gate` 与项目模板同步。

## 0.0.47 — 2026-09-13

**0.0.46 三语核对，修正 4 处译文问题和 1 处中文自身的漂移。** 派四个 agent 逐段核对
`evidence-discipline.md`、`show-me-test.md`、`test-discipline.md`、`verify-before-claiming.md`
的中英日三语是否语义一致（show-me-test 未发现问题）。修正：
`verify-before-claiming.md` 英文丢了一处粗体强调（「不等于你知道它定的是什么」）；
同一篇「别人提出的事实性更正」被英文和日文都窄化译成了「the user / 利用者」，
与 `pushback-discipline.md` 里同一句的译法（someone else / 他人）不一致，改回一致；
`test-discipline.md` 英文把「作废条款」（discard clause）与「失败条款」（failure clause）
两个不同术语混成了一个词，改回 `discard clause`；
`evidence-discipline.md` 日文「门禁」一词前后分别译成「門番」和「ゲート」两种说法，统一成「ゲート」；
英文「判据」一词有一处走样译成「Test」，改回「Criterion」。
另发现中文源文自身的漂移：「自问表」在历次追加后已有 6 行，正文却仍写「这四条」，改成「这六条」。

## 0.0.46 — 2026-09-13

**四条规则补七处 2026-09-13 的实测教训，门禁不动。** `evidence-discipline.md`：「不许先有结论再建模型」的自问表加一行
（这个数是不是某个没扫过的参数的函数）；「臂的定义也在跑前写死之列」补一段——跑产物之前修订臂或判据合法，但要在跑前登记里
写明改了什么、依据哪个单测读数、时点在产物之前；新增一节「判据自己也会写错：打中之后先判是哪一种」，列打中不分辨臂、
判别子观测不到、打中归错了判据三种形态，以及打中之后先问的三句。`test-discipline.md`：失败条款一节下新增小节
「判据不写成合取，门槛不能是臂定义的同义反复」；新增一节「端点不是轨迹：被条款当谓词输入的量，实验要报轨迹」。
`show-me-test.md`：跨装置的检查也可能只钉住了值、没钉住量——两个量就登记两个名字，各钉各的值。
`verify-before-claiming.md`：截断过的输出也是「窄的那一句」，要说「只有这几处」输出不许截断，嫌长先数。

## 0.0.45 — 2026-09-11

**`gate.sh --staged` 跑到一半被打断也清掉临时 worktree。** 此前 Ctrl-C 打断时 worktree 留在仓里，下一次要手工
`git worktree prune`；现在建好 worktree 之后挂 INT / TERM 的 trap，清掉再退出。selftest 加一例：睡 20 秒的项目阶段
跑到一半给整组发 INT，打断之后仓里只许剩一个 worktree 登记；删掉 trap 这一例判红。

## 0.0.44 — 2026-09-11

**`gate.sh --staged`：只拿「HEAD + 暂存区」跑整道门禁。** 几个会话共写一个仓时，工作区里混着别人没收尾的改动与未跟踪文件，
门禁在工作区上判的红分不清是谁的——`session-wrapup.md` 第 4 条此前只能让人逐个文件去对。现在在临时 worktree 上把
`git diff --cached` 套到 HEAD 上再跑一遍整道门禁：别人的未提交改动与未跟踪文件都不进来，红的就是这一次提交带进来的；
规范副本不进 git 时原样拷进 worktree，兄弟目录里的上游照旧从原来的项目根找。起因是 singlefs 2026-09-11 的一轮收尾：
工作区上的门禁红了五个阶段，其中三个是另一个会话的，靠手工搭 worktree 才分清。selftest 加三例：没暂存的违规在
`--staged` 下不算；不带 `--staged` 时同一处要算进来（证明那个阶段本来会红，少了这一例，`--staged` 的绿分不清是没算进来
还是阶段根本不红）；暂存了的违规算。`session-wrapup.md` 第 4 条第二款补一句用法。

## 0.0.43 — 2026-09-11

**`i18n-sync.sh --update` 自己刷新译本仓的 `SOURCE-MANIFEST.sha256`，不再手抄。** 发 0.0.42 时，
`--update` 因为 en / ja 的 SOURCE-MANIFEST 还是旧清单，整个被「落后」拒掉，共享部分一份都没同步过去——
那一步以前靠人记得把 zh 的 `MANIFEST.sha256` 抄过去。现在 `--update` 碰到 SOURCE-MANIFEST 落后或缺失，
先逐篇核对译文的溯源标记：全部对上当前源文就照本仓清单刷新，抄完回读；有一篇缺、没盖章或译自旧版源文就不抄，
列出是哪几篇。逐篇溯源的判据收成一个函数 `stale_articles`，这一步和原有的第 5 项逐篇比对共用它。
两处教人手抄的「怎么办」改成跑 `--update`，三个 README 同步改了说法。`selftest.sh` 加 5 个用例：
重译并盖章后会自己抄且与清单逐字节一致、没重译时不抄且 SOURCE-MANIFEST 一个字节没动、缺了会补上。

## 0.0.42 — 2026-09-11

**推送改成全部语言仓一起验、一起推。** 0.0.41 只推了 zh，en / ja 各落后两个提交：远端上三种语言说的不是同一版规矩，
而门禁只在本地跑、看不见远端，没有任何东西报警。新增 `scripts/push-all.sh` 与 git 钩子 `scripts/githooks/pre-push`：
每个语言仓各设一次 `core.hooksPath`，之后推任何一个仓，都先验全部语言仓（在 master、工作区干净、VERSION 相同、
各自门禁全绿），全过了先推其余语言仓、再放行本仓；任何一项不过就一个都不推，推到一半被拒时报出已推的是哪几个。
在 git worktree 里推时，git 给钩子设的 `GIT_DIR` 是本仓的绝对路径，会压过 `git -C`，脚本开头先清掉，
不然查验和推送其余语言仓时实际落在本仓上（普通仓里推，钩子里没有这个变量）。
`selftest.sh` 加 10 个用例，走真实的 `git push` → 钩子路径，拿本地裸仓当远端。`CLAUDE.md` 补一句怎么启用。

**规则补四条，来自 singlefs 这一轮踩的坑。** `command-safety.md` 新增「带闸的脚本，判完了才交出产物」：
先输出、后判，调用方的重定向文件里就留下一份被判作废的产物，看着和合法的一样。`evidence-discipline.md` 补两条：
臂的定义里「怎么做」要推得出「做完会怎样」，推不出来，各条腿按不同的句子判的就是两条不同的臂；
扫描出来的极值落在区间端点，就不是量到的数，只能写成「≤ 端点」。`session-wrapup.md` 第 4 条从三个前提加到四个：
新建文件可能盖掉另一个会话刚写的未提交文件，要用排他方式新建。

## 0.0.41 — 2026-09-10

**新增 `rules/code-discipline.md`：machine-first 的实施细则。** 原则只有一条：不让过去的最佳实践替我们做决定。
`machine-first.md` 讲了为什么要重审老规矩、怎么审，没写审完之后代码怎么写；命名那条（名字不限长度、不用缩写）
写在 `engineering-philosophy.md` 里，是 review 时看的一段说法，不是规则。新的一篇把这些落成细则：名字、分支、类型、
错误、函数与嵌套、注释，外加一张按出处分组的表——《Clean Code》、SOLID、《重构》、《程序员修炼之道》、
Rust API 指南、Linux 内核编码风格等流行做法 85 条、分 10 组，逐条给出处置和我们的写法。
代码层面的审视结果从另两篇挪了过来，那两篇只留理念与设计、流程层面的表；挪过来的示例里的缩写名
（`dev`、`m`、`Lba`、`commit_txn`、`num_`）一并写全。

同一版里按审阅意见收紧了六处说法：路径数只量控制流，不是验证难度的全部，循环的迭代上界、跨轮状态、
提前出口要另外写清；禁通配臂的对象是封闭集合，语义上允许未知值的把「未知」做成显式成员，`match` 照样穷举；
「一个概念一个名字」的单位是语义概念，不是词；名字不靠模块路径补全，写明这是有意让名字带命名空间信息；
`as_` / `to_` / `into_` 是约定不是保证，以 C-CONV 的表为准；错误成员按调用方要做的决定分，不按底层原因逐个展开。
`engineering-philosophy.md`、`machine-first.md` 里「路径数有限就能验」的说法跟着改成必要条件。

**新增门禁阶段「命名纪律」（`scripts/naming-lint.sh`）。** 扫项目里每个 `.rs` 中我们自己声明的名字，
判单字母和常见缩写（词表 144 条，每条带「该写成什么」）。项目在 `.claude/abbreviations` 登记领域缩写，
也可以按类登记自己的编号（`e<数字>`）；在 `.claude/naming-lint-exclude` 声明不扫的目录或单个文件（回扫旧代码时按文件列、改完一个删一行）；两份都要写理由。
行内豁免是 `// naming-lint:external <理由>`。trait 实现的方法名、`extern` 块、Cargo 定的文件名不判。
shell 脚本的名字还没做成检查，`gate.sh` 把它列在未实现的阶段里。
样本 12 组；在副本上做了 39 条变异（去掉一条豁免、一类声明处、一步剥注释或字符串），全部被样本抓到。
拿 singlefs 的 research 代码试跑（119 个文件、16792 个名字，未登记任何缩写）报 7246 处；
抽看各类命中，两类误报已修：Cargo 定的 `lib.rs`，名字中间的英语冠词 `a`。

**`scripts/check.sh` 的 clippy 多带七条编码纪律的 lint**：`wildcard_enum_match_arm`、
`allow_attributes_without_reason`、`cast_possible_truncation`、`cast_sign_loss`、`cast_possible_wrap`、
`undocumented_unsafe_blocks`、`shadow_unrelated`。在样本 crate 上实测：每条各放一处写错的，七条都红；
照细则写的版本，格式、clippy、构建、单测四步全绿。

**`doc-discipline.md`、`writing-economy.md`、`writing-style.md` 合并成 `rules/writing-discipline.md`。**
三篇管的都是写给人读的文字（写给谁、多长、怎么说）。合并稿逐行对过，三篇的正文一句没丢，
只删了三篇之间互相指路的话。引用它们的地方一并改指新文件。

**doc-lint 的规则清单检查扩到模板与项目。** 原先只查 SOP 仓的 `CLAUDE.md` 与 `rules/` 逐项相等；
现在 `templates/CLAUDE.project.md`，以及项目的 `CLAUDE.md` 对装进来的副本，也按同一判据查。
逼出这一改的是两处实测缺口：模板从 0.0.31 加入 `pushback-discipline` 起就没引用过它；
singlefs 的 `CLAUDE.md` 从来没 @ 过 `engineering-philosophy`、`sop-first`、`pushback-discipline`、`writing-style`，
这四条在 singlefs 的会话里一直没读进上下文。模板已补齐。

`GLOSSARY.md` 改了「缩写」「路径数」的说明，新增「缩写登记表」「通配臂」两条。

## 0.0.40 — 2026-09-10

**新增门禁阶段「CHANGELOG 连续」（`scripts/changelog-lint.sh`）：每一版都要有自己的一节，最新一节就是 VERSION。**
版本纪律只管「改了规范本体就抬 VERSION」，不管 CHANGELOG 跟没跟上。实测：0.0.39 那次 VERSION 从 0.0.35
抬到 0.0.39，三个语言仓的 CHANGELOG 一起跳过了 0.0.36，那一版的两处改动没有任何一节记着；全部门禁照绿，
是逐段对 diff 才看出来的。

判的是整份文件，不是 diff 窗口：二级标题只许是 `## x.y.z — YYYY-MM-DD`，最后一节可以是「x.y.z 及更早」
这种不带日期的收尾；最新一节等于 VERSION；相邻两节必须是紧后一版（补丁 +1、次版本 +1 且补丁归 0、
主版本 +1 其余归 0），跳号、重复、倒序都红；一节都没有也红。只在 SOP 仓自己跑，各语言仓各判各的 CHANGELOG。
`CLAUDE.md` 首屏补了一句，`skills/gate/SKILL.md` 的「只在 SOP 仓自己跑的阶段」加上了它。

## 0.0.39 — 2026-09-10

**`rules/verify-before-claiming.md` 新增一节：核了窄的那一句，说出口的却是宽的那一句。**

原来那一篇管两件事：说外部状态之前现查；以及「它定了没」和「它到底怎么定义的」是两个问题。
两条都假定**没查**是失败模式。这一节管的是**查了也失败**的那一种——
你确实跑了命令、读了文件，手上是一条被核实的命题，**错在说出口的那句比它宽**。

判据：说出口的那句话，它的主语和范围，是不是就是刚才核的那一个？
核的是「这条路不通」，只能说「这条路不通」，不能说「做不到」；
核的是「这个文件里没写」，只能说「这个文件里没写」，不能说「全仓没有」。
做法是把那句宽的话当成待证命题，问「要让它为真，需要排除掉哪些情况」，逐个排除；
排不完就把话缩回真排完的那个范围。

实测于 singlefs（2026-09-10，同一天同一个人两处）：
① 核到「`sudo` 被沙箱拦下」⇒ 写下「拿不到 root，这个观测做不了」，
而同一个仓里另一条路**不需要那个权限**、文档里逐字写着「已实测通过」
⇒ 整轮零观测，而记录在案的理由是假的。
② 核到「某条类规则的枚举里没列这个对象」⇒ 写下「这一格全仓没有落点」，
并据此立了一整个实验的题；而**类归属写在另外两个文件里**，那条类规则本来就罩着它
⇒ 立题作废，同一笔欠账连写错两次。

⇒ 该节末尾另附一条：「一个对象有没有被条款罩着」尤其容易中招，
因为**条款可以写在别处、按类罩着它**，而它自己那一节一个字都没有；
判它之前拿它的名字全仓搜一遍，看它有没有被归进某个已有的类。

## 0.0.38 — 2026-09-10

**两条各自补在已有的一节里，都是「条款还在、但已经不起作用」这一族。**

**`rules/evidence-discipline.md`「重跑之后要回对正文」补第二种形态：不是数漂了，是限定词没了。**
原来那一节只管数值漂移——正文写 1.475× 而产物是 1.625×，区间断言罩不住。
但正文与产物对不上还有一种：**每个数都是真的，而那句结论比产物支持的范围宽**，
因为正文那张表把一个参数维度整个脱掉了。数漂了还有两个数可以并排对，
限定词没了**连对照物都没有**。判据是：结论说「只有 A 才买得到」时，
去产物里读**非 A 的臂在每一个参数点上的值**；正文那张表没有那个参数的列，那句「只有」就不成立。

实测于 singlefs（2026-09-09）：一份实验正文写「真正只有聚集才买得到的是回收那一格……
另两条臂一个都腾不出来」，而同一份留存产物里另一条臂在不交错那一格上与它**逐格相同**；
正文那张表没有交错步长这一列。**装置的断言射程一直是对的**——单测写死了那个参数，
断言消息里就带着「交错之后」——脱掉射程的是正文。复跑逐字节一致、**门禁全绿**。
那句结论已被三处下游继承，其中一处是靠它定案的那条决策分项本身。

**`rules/test-discipline.md`「失败条款不许让结论不可证伪」补反面：前件可以写反，写反之后永不发火。**
主结论管的是「条款让结论不可推翻」；这一条管的是「条款自己不可能被触发」。
写反不长成「错了」的样子，长成「**没触发**」的样子——而在轮次报告里，
「这条条款没触发」与「这条条款检查过、结论没问题」看不出差别。
做法：**每写一条失败条款，紧接着写一句「什么观测会让它触发」**；写不出就是写反了或写空了。

实测于 singlefs（2026-09-09）：一条跑前写死的条款写「某条臂的收益只在**不交错**负载上出现
⇒ 记条件未知」，而那一轮的发现是收益只在**交错**负载上出现 ⇒ 前件恒假、一次没触发，
而它本该触发。写条款的人自查两遍都没看出来，是另一条腿逐字核前件时打中的。

## 0.0.37 — 2026-09-09

**`rules/evidence-discipline.md`「不许先有结论再建模型」补一节：臂的定义也在「跑前写死」之列，
以及失败条款打中时的唯一合法出路。**

此前的条款只给了禁令——判据、阈值、作废条款跑前写死，「不许回头改判据」「不许事后把规则改宽
再宣布它赢」——但**没给合法出路**。于是失败条款真的打中时，只剩两条路：装作没触发，或整轮作废。
而臂本身写含糊时，攻击会打中它**最弱的读法**，顺手把臂「澄清」成强的那个再宣布它赢，
形式上判据一个字没改，实质是事后建模。

补的三步是：按最弱读法**记一次输**、新形态**只许收严**（要被原来那套判据判得更严或相同）、
**写明收严在哪**（说不出新形态多要求了什么，那就是放宽）。并点明第一步最容易省，
而省掉之后，诚实的收严与「改宽再宣布赢」在文本上长得一模一样。

实测于 singlefs（2026-09-09）：一轮三方论证里，反推腿按跑前写死的失败条款判一条臂出局，
理由是它罩不住「字段顺序」。核下来那条臂的原文并没有说过它只投影标量——打中的是最弱读法。
判决按三步走完，收严后的形态写进了那条决策正文。

## 0.0.36 — 2026-09-09

**`rules/evidence-discipline.md`「撤回一个数或一条结论，同样要当场回扫谁在引它」之后补一节反面：
撤回的理由失效，不等于被撤回的结论自动回来。** 撤回是一次判决，理由塌了只说明这次判决的依据没了，
不说明反面成立，要让原结论回来得重新论证。而且撤回之后那一格通常已经有另一条依据顶上来，
只盯着被撤回的那条会把它整个漏掉。做法是按顺序问三句：今天顶在这一格上的是哪条依据、
它自己站不站得住、复活所需的重新论证做了没有。

**`skills/decide/SKILL.md` 新增第 7 条：待定项的问法必须只有一种读法。**
把每一种读法各写一遍，答案不一样就是问法还没定完——先定问法，再谈依据。

实测于 singlefs（2026-09-09），两条出自同一格：一条依据 2026-09-06 因「与 X 互斥」被撤回，
X 次日取消，提问者据此要求重判，方向指着复活。三方论证的反推腿打中的三处，都与「互斥还在不在」无关：
另一份文档当天就留了一条不依赖那个互斥的替代依据；那条替代依据的两个支点各断一处；
而这一格问的根本不是被撤回那条依据回答的事——问法「要不要同等完整性」有两种读法
（问它自己的字节，还是问它指向的目标），被撤回的依据答的是前一种，措辞问的是后一种。
那一格卡了三天、走了两轮论证，问法定死之后当场收口。

## 0.0.35 — 2026-09-06

**`rules/show-me-test.md`「踩过的坑要做成会失败的检查」补一节射程：立在一个装置里的检查，
管不住下一个装置。** 断言与变异测试只在它所在的那套装置里生效；换一套装置把同一件事
重新建一遍模型，那条检查一个字都不会说。

实测于 singlefs（2026-09-06）：一个计数模型把「容量 × 填充率」那笔预算当成其中一类对象的数量、
再往上加另一类，等于往盘上摆了 126% 的对象；当天修好并留了会红的断言。几小时后另一个模型
重犯同一个错（135%），那条断言在别的装置里，一声没吭——新装置自己单测全绿、变异全抓、门禁全绿，
两个模型对同一个物理量报的数差 1.5 倍，没有任何东西去对过，错的那个数还写进了一条已定条款。

判据因此从「这个坑做没做成会红的检查」改成「**这个坑在哪一层**」：坑在一段代码的行为里，
检查立在代码旁边就够；坑在**建模口径**上（同一个量该怎么算），就必须立成**装置之间的交叉断言**。
快速判据是「换一套装置从头再做一遍，还会不会再踩」。并点明修坑的人最容易在这里收手——
他确实把坑做成了会红的检查，证据齐全、门禁全绿，所以不会再想一遍那条检查的射程。

**`rules/evidence-discipline.md`「引产物就整行抄」补一节：重跑之后要回对正文，复跑绿不代表正文对。**
复跑钉的是结论落在哪个区间，区间有多宽，正文就能在里面漂多远。实测：一份实验正文写
1.475× / 22.33× / 5 123 506 ns，而它自己的留存产物逐字是 1.625× / 30.36× / 5 199 857 ns，
三个数全在区间断言之内，门禁全绿。同节顺带管住「N 个单测 / M 条变异 / 产物 K 行」这类
可以被一条命令数出来的声明——同一天在同一个仓里查出五处与源码或产物对不上。

两处都只加规则本体，没有新增门禁阶段：跨装置交叉断言要先给产物定一套「这个数是什么量」的
机器可读标注，本包里没有；计数声明的机检落点是各自项目的本地阶段。

## 0.0.34 — 2026-09-06

**`rules/evidence-discipline.md`「新立一条判据就回扫已有条目」补一节：撤回一个数或一条结论，
同样要当场回扫谁在引它。** 同一条纪律换个对象，但更隐蔽——撤回的人通常确实扫了几处，
于是有理由认为扫完了，漏掉的那处要过几天才发作。

实测于 singlefs（2026-09-06）：一个位置条目的宽度被改写时，那条决策正文自陈「本项定的是
一个此前从没走过决策流程、却已经被消费掉的数」，并当场点名扫了下游两个派生数；三天后另一份
文档里第三处引用仍写着旧值，被抄进一次三方论证的背景材料，三条腿一起继承，凡踩在那个数上的
论证整轮作废。撤回的人扫了两处、漏了一处，而他并不知道自己漏了。

判据写成「撤回之后答得出一份『全仓还有谁在用这个数』的清单才算撤完」，并点明扫描范围是全仓
不是记忆——记忆挑出来的正是好找的那几处。同时写明它与既有的「喂给多方论证的背景材料本身要先核」
是同一个洞的两端：一端是引的人没现查，另一端是撤的人没扫干净，两端都堵才堵得住。

规则本体只加一节，没有新增门禁阶段：这条今天还没有可机检的形态（要判「这个数被撤回了」需要
一份撤回登记，本包里没有）。使用方要机检的话，落点是各自项目的本地阶段。

## 0.0.33 — 2026-09-06

**`install.sh` 学会分辨「项目接管了这份文件」和「项目落后于上游」。** 此前它只判
「与上游模板不同」，两种情形一模一样。而 kb 骨架、skill 桩本来就是给项目改的——
于是**任何一个动过自己 kb 的项目，装完第一次之后版本戳就再也刷不动了**，
门禁阶段 0 从此长红。实测于 singlefs：12 份「落后」全是项目自己改的
（`.claude/kb/INDEX.md` 有 36 行是项目自己的写法）。

项目把接管的那几份写进 `$ROOT/.claude/install-owned`，一行一条 `<相对路径>  # 为什么`。
**理由不许省**：接管一份文件的代价是此后上游对它的改动都不会再送到，
理由那一栏就是让人正面对上这一点。清单一律报进输出——静悄悄少比几份，
和这道守卫没实现长得一模一样。

两条会红：没写理由；写了 `install.sh` 根本不铺的路径（那种条目一点作用都没有，
却让人以为那份文件已经被豁免了）。**清单坏了当场挡住，不许接着刷版本戳。**

`README.md` 里「版本戳每次都刷新」那句是错的，一并改掉——它和 `install.sh` 的实际行为
早就对不上了。

5 个变异逐个跑过。其中一个暴露了盲区：把「清单坏了就挡住」整段删掉，原有两个样本
**一起全绿**——它们同时还有内容落后，是那条路让它们红的。补了一个没有别的落后的场景
才隔离出来。自检 182 → 188 例。

## 0.0.32 — 2026-09-06

**gate-lint 补上第三种拒绝形态：直接打印的 `✗`。** 此前它只认 `lib.sh` 的 `bad` 与 `die`，
而**项目本地的阶段多半不 source `lib.sh`**——它们直接 `echo "  ✗ …"`，或者把判据写在
内嵌的 python 里 `print('  ✗ …')`。这一整类拒绝一条都没被查过。

实测于 singlefs 的本地阶段：46 处这样的拒绝全部免检，补上检查后 **14 处一句下一步都没有**
（`15-research-build.sh` 的「没有 cargo」、`70-citations.sh` 的「找不到 $S」这种，
都是直接 `exit 1` 而不告诉人怎么办）。gate-lint 覆盖的拒绝数 125 → 170。

两处判据是照真实语料定的：

- **窗口不按 5 行算，按「到下一处拒绝之前」算。** python 常常先打一句 `✗`、再循环列明细、
  最后才给出路，5 行窗口会把这一批整体误判成没出路（样本 `printrejok` 盯着它）。
- **heredoc 体默认不当代码读，但解释器 heredoc 要读。** `python3 - <<'PY'` 里的 `print`
  是真的会打给提交者看的；照旧跳过的话，python 写的阶段仍然整体失明
  （样本 `printrejpy` 盯着它）。

两种豁免都要显式写出来，猜不着的东西不猜：循环里逐条列明细的行标 `# gate-lint:detail`，
汇总行标 `# gate-lint:summary`。

`rules/sop-first.md` 的拒绝形态表加了第三行。样本 4 个，4 个变异逐个跑过，
每个恰好被一个用例抓到。自检 178 → 182 例。

⚠️ **这条会让 singlefs 的门禁红 14 处**，都在它自己的 `.claude/gate.d/` 里，
上游三个仓不受影响（共享包里一处直接打印的拒绝都没有，全走 `bad`）。

## 0.0.31 — 2026-09-06

**新增规则 `rules/pushback-discipline.md`：提议不因为来自谁而免检。** 用户提的方案，
和别处来的方案过同一道闸。和实测数据、或者已经核实过的事实对不上时，动手之前就要说清
三样：对不上的是哪一条（带出处）、按提议做会出什么问题（什么现象说明它出现了）、
有没有第三条路。对方明确重申就照做、不再劝、不许消极执行，但**警告必须留下来**。
**做完了才发现对不上，照样要说，该撤就整个撤回**——成本由用户承担，代码只对正确性
负责，「已经做了这么多」不是继续的理由。代码改动里没有撤不回来的事：撤回的代价过去是
人的时间，现在那部分是机器做的，几万行和几百行没有量级差别。真正退不回去的是
`git checkout`、`rm -rf` 与对外的永久承诺，`command-safety.md` 管那一类。

**警告落在 `.claude/warnings/<日期>.md`**，一天一个文件，一次警告一个 `##` 小节，
四项齐全：提议 / 异议 / 已知风险 / 结果。别处一律链过来，不抄第二份
（`kb-discipline.md` 第 4 条）。按日期分文件是为了**不用回头改旧文件**——警告写下就是
当时的记录。`install.sh` 会在项目里铺出这个目录。

**doc-lint 新增两项检查**：

- `.claude/warnings/` 下的文件名必须是 `YYYY-MM-DD.md`，每个 `##` 小节四项要齐。
  项名按语言取，三种语言都跑：它认的是固定项名，不是靠字形黑名单猜语义，
  所以不进「没有词表就未实现」那一档。
- `rules/*.md` 与 `CLAUDE.md` 里的 `@rules/` 引用必须逐项相等。两个方向都无声：
  没被引用的规则不会进上下文，却和生效的规则长得一模一样；引用了不存在的文件，
  那一行只是不展开，CLAUDE.md 读起来照样完整。这条正是这一轮自己需要的——
  新加一份规则而忘了写进 CLAUDE.md，此前一点也不会红。

样本 7 个；5 个变异逐个跑过，每个恰好被一个用例抓到。自检 171 → 178 例。

## 0.0.30 — 2026-09-06

**en / ja 两仓的门禁修红。** 两处，都是「一份规则复制成多语言仓」漏掉的口子：

- `doc-lint` 的样本是中文写的，而 `scripts/` 逐字节复制进每个语言仓。于是在 en 仓里，
  这批样本被拿去跟 `## Revision history` 对判——44 例一起假红，而 doc-lint 自己一个字都没坏。
  语言是**样本的属性**，不是仓的属性：`selftest` 现在用 `DOC_LINT_LANG=zh` 钉住样本的语言。
  这个口子能换掉判据，所以**设了就打印出来**，并有一条自检盯着那句话还在不在。
  0.0.25 按语言取判据时留下的，en / ja 从那时起一直红着。
- en 仓把 `templates/kb/` 里 D1 与 E1 的占位简称都译成了 `<name, 24 characters or fewer>`，
  撞在「一个简称不许两个编号共用」上。改成各带名词；zh / ja 的译法本来就是分开的。

**doc-lint 新增排除清单：原样保存的证据不许事后改。** `research/prompts/` 这类目录存的是
当时原样发给模型的提示，与产物一一对应，事后改一个字，产物就不再对应它的输入。
项目在 `.claude/doc-lint-exclude` 里声明，一行一条、**每条要写明为什么**；指向不存在的目录、
或者一个文件都没排到，都判红——不起作用的排除项会让人以为那批文件已经被绕开了。
排除项一律报进输出：静悄悄少扫两百个文件，和这道检查没实现长得一模一样。
规则见 `rules/evidence-discipline.md`；七个样本，含一个不带排除声明的对照组。

## 0.0.29 — 2026-09-06

- `doc-lint.sh`：「已被 X 覆盖」这条的编号前缀从任意大写字母收窄到 `D/E/C/I/A/O`。
  实测误判：一句讲盘上字节的话「要重建 U1，需要 U2 的原始字节 —— 已被 U9 覆盖」被判成
  「正文不许就地标注被某条覆盖」。那句里的「覆盖」是字节被写掉，不是条款被推翻，
  两个义项撞在同一个词上，只能靠「被覆盖的那个东西是不是一条编号条款」来分。

## 0.0.28 — 2026-09-05

**计数判据认 python 的 f-string。** 只认 shell 的 `$n` 时，用 python 实现的阶段
整批假红——它们的成功摘要在 python 里格式化（singlefs 的两个阶段实测）。
样本 `countpy`；`nocount` 仍红，判据没有被掏空。

## 0.0.27 — 2026-09-05

**修 0.0.26 自己引入的一处无限循环，并给 selftest 加超时保护。**

- `gate-lint` 挖算术展开时，配对的 `))` 只在 `$((` 之后找。在整行里找第一个 `))` 会挂死：
  `if ((okc)); then …; pass=$((pass+1)); fi` 这样的行，第一个 `))` 落在 `$((` 前面，
  切完的串仍含 `$((` 且不变短。真实语料上当场无限循环——跑了 9 分钟没结束，
  既不红也不绿。样本 `arith2` 必须跑得完且是绿的。
- `selftest` 的两处样本执行点加 `timeout`（默认 60s，`SELFTEST_TIMEOUT` 可调）。
  **挂死的检查比红的检查危险**：它在门禁输出里什么都不是，只是永远不回来。

## 0.0.26 — 2026-09-05

**项目本地阶段第一次被门禁自己管起来，并补上三条从 singlefs 三天作业里剥出来的纪律。**

一、**`.claude/gate.d/` 纳入两个 lint**

`gate.sh` 把项目本地阶段目录一并交给 `gate-lint` 与 `shell-lint`。此前它不在任何一个
lint 的扫描里——那些脚本和共享阶段一样会拒绝提交者，却从没被查过：一喂就是 7 条
没有出路的拒绝（singlefs 实测）。

二、**gate-lint 新增第三条：成功摘要要报出检查了多少项**

「扫到 0 项」不是通过。判据的搜索范围写窄一点，对象就全被第一步跳过，
既不算通过也不算失败，末尾照样报绿（singlefs C114：一个阶段的第 3 项就这样绿着）。
只对**报了成功**的脚本判，确实不扫对象的写 `# gate-lint:nocount <理由>`。
样本 `nocount` / `countok`；本包自己的 `show-me-test.sh` 是第一个被它咬到的。

三、**gate-lint 修一处假红**：`bad=$((bad + 1))` 里的 `bad ` 紧跟 `(`，按 CMD_POS 读
成了一处拒绝。判定前挖掉算术展开；样本 `arith`。

四、规则增补（都带实测代价，各一节或一行）

- `evidence-discipline`：**引产物就整行抄**（同一处差值连错四轮）；
  **新立判据当场回扫已有条目**（尺子只挥向新来的那个）；
  取样表加一行「同一份产物里有没有反例」；稻草人臂那节加「引某条臂的数要带它的定义」。
- `show-me-test`：会红那条**同样管设计论证**——对抗轮打中的每一条，先做成必报非 0 的世界再改规则。
- `skills/decide` 硬要求第 6 条：**动了由人拍板的那一句，要在 `checks-owed.md` 记一笔未还的账**。

⚠️ **本轮发现、未修**：译本仓（en / ja）自己跑 `gate.sh` 不是绿的——`selftest` 有 44 处判错。
`i18n-sync` 只在参照仓跑，译本仓的门禁此前没人跑过。归因已做：拿 0.0.23 的英文 rules
配当前脚本跑是 47 处，比现在还多 ⇒ 与 0.0.26 的改动无关，是包的既有状态。
病灶是 selftest 里那批「拿仓内真实语料喂检查」的用例——它们的判据按中文语料写，
对译本仓成不成立还没判过。修它要先回答这个问题，所以不在本轮顺手改。

## 0.0.25 — 2026-09-02

**译本仓里最后一批中文清掉，`agents/` 这一层立起来。** 两件都落到会红的检查上。

一、**skills / templates 从「原样复制」改成「逐篇翻译」**

此前它们在 `i18n-sync` 的 `SHARED` 里逐字节复制，于是日语仓的 skill 正文、项目骨架、
litmus 注释全是中文——而 `templates/CLAUDE.project.md` 会被 `install.sh` 写成使用者
项目根的 `CLAUDE.md`。与「用日文的不必读中文」直接冲突。

- 清单从 14 条扩到 28 条：`CLAUDE.md`、`rules/`、`agents/`、`skills/`、
  `templates/`（含 `*.litmus`）。判据一条：**里面有没有面向人的散文。**
- **新增覆盖率检查**：两边都不沾的文本当场红，`howto` 逼你二选一。
  这是「空缺」的可机检形态——新加一份 `.md` 而忘了决定它翻不翻译，门禁替你想起来。
- **溯源标记的位置与包裹形式按文件类型定**：`SKILL.md` 的 YAML frontmatter 必须从
  第 1 行起（拍在它前面 skill 安静地装不上）；`.litmus` 第 1 行必须是 `C <名>`，
  herd7 报 `splitter error in sublexer first line`（跑 herd7 实测）。
  盖章后回读确认，并做成会红的检查。
- **`install.sh` 铺进项目时剥掉溯源标记**：那是分发层的账，抄进使用者项目就是一条
  永不更新的陈旧标注，而且贴在他马上要改的文件上（装一遍实测到）。
- 28 篇译文（en/ja 各 14 篇）全部重写并盖章。三语的 litmus 经 herd7 实跑，
  判定一致：Never 0 3 / Sometimes 1 3。

二、**`agents/` 纳入治理**

- `agents/INDEX.md` 写明这一层放什么（与 `skills/` 的分界：读的还是委派的）、
  怎么写、三条纪律、怎么接。进清单、逐篇翻译。
- 进 `GOVERNED`（改了要抬 `VERSION`）、`install.sh` 铺 agent 桩（与 skill 同构）。
- **空是状态，要说出来**：目录为空时 `manifest.sh` 显式报「已纳入治理，当前为空」，
  不静默通过。
- doc-lint 新增：agent 定义不留历史节；必须有 `name`（与文件名一致）与 `description`——
  两样都不会报错，只会安静地不生效。

三、顺带修的

- `manifest.sh` 的 `gen()` 在管道里丢了 `cd`——从仓根跑时 cwd 恰好就是包根，看不出来，
  selftest 拿样本仓一喂就露了。
- `translated_paths` 里 `find` 撞上不存在的目录整条失败：`2>/dev/null` 只藏消息不藏
  退出码，`--update` 退出码 1 而一个字都不打印。
- `stamp_read` 原先用 sed，两种包裹形式要写交替，而 BRE 的 `\|` 跟分隔符撞车——
  它静默匹配不上，靠盖章后的回读断言才抓出来。改用 awk。
- **GLOSSARY 的语言缺口补掉了**：说明列改成三语并列（`zh<br>en<br>ja`）。
  它仍然不翻译——**它本身就是对照表，抄成 N 份必然漂移**；而说明是面向人的散文，
  必须让读它的人读得懂。两件事在同一张表里各按各的规矩办。
  `doc-lint` 判「说明列不是三语」：段数不等于 3、或某段为空，都红。
  样本里四种坏法（1 段 / 2 段 / 3 段但中间空 / 4 段）各踩一条判据，互相盖不住。
  **本包至此没有已知的语言缺口。**

判别力：用例 111 → 120。新加的检查逐个变异验过，改前全绿、改后全红——
其中五条是本轮自己制造的新盲区（覆盖率检查、第 1 行被压住、install 剥标记、
agent 的 name、`GOVERNED` 的 `agents/`），补样本后才咬得住。

## 0.0.24 — 2026-09-02

**这一版没有单独发布过，和 0.0.25 一起在同一次提交里出去。**
下游看到的版本会从 0.0.23 直接跳到 0.0.25，这里记下来免得有人去找 0.0.24。

第二轮对抗审计（变异测试驱动）后的整批修复。**主线是判别力**：上一轮补的门禁里，
有六条删掉之后 selftest 仍然 54/54 全绿——检查在，盯着检查的东西不在。

- **判别力**：doc-lint 的历史陈述模式组 / CLAUDE.md 历史节 / kb 历史节、
  gate-lint 的 howto 窗口值、lkmm 的两条静态检查、show-me-test 的标注广度，
  各配单一职责样本；`blind` 的 `want` 从「正文不许」（六类检查共用）拆成逐条。
  八个变异现在全部转红。用例 54 → 69。
- **doc-lint 的死模式**：`'[〔【\[]已废弃[〕】\]]'` 在 GNU grep 下永不命中——
  POSIX 规定方括号集合内的反斜杠是字面量。改成 `'[[〔【]已废弃[]〕】]'`。
- **die 不再免检**：`die` 是 `bad` + `exit`，同样是拒绝。lib.sh 的 `die` 改成
  「消息 + 出路」两段，gate-lint 判「只带一句话的 die」，17 处调用点补齐出路。
- **新增 shell-lint.sh**：把 command-safety 里两条可机检的做成会红的项——
  按模式匹配杀进程、子 shell 里的赋值往外带值。
- **qemu/run.sh 的 `$QEMU_LOG`**：只在 `$( )` 里的函数内赋值，父进程六处引用
  在 `set -u` 下是 unbound variable，**五处失败分支在打印 howto 之前就被带走**。
  改成工作目录由调用方持有。这正是 command-safety 自己写下的那条坑。
- **gate skill 的「证明它会红」示例实测退出码 0**：`>>` 追加落在「## 历史版本」
  之后，而正文扫描在那里停机。换成另起目录的样本。
- **规范本体只定义一次**：`version-discipline.sh` 的 `GOVERNED` 为唯一权威，
  CLAUDE.md / README / session-wrapup 三处各自的清单改成链过去；
  README.md 与 I18N 纳入治理（此前 README 载着规范性内容，doc-lint 扫得到它，
  但版本纪律与清单对账都够不着）。
- 去重与陈旧：CLAUDE.md 与 README 重复且已漂移的多语言段（「不必读英文」/
  「不必学英文」）合并到 README；README 三节讲同一件事合成一节；
  install.sh 的「三件事」实为六样（含写到项目根的 `litmus/`）；
  gate skill 的阶段表 4 行补到 9 行、假失败表里那句不存在的消息换掉；
  「三语」写死的 15 处改成与 `I18N` 的 `languages=` 一致的说法。
- gate.sh：「未检查副本是否落后上游」进入汇总的「本次未跑」——此前只在开头
  warn 一行，末尾却报「N 个阶段全部通过」。

**本轮复核后的第二批**（两个审核 agent 各查一遍，逐条现查过才收）：

- **en 版 `show-me-test.md` 丢了一个否定**，「不保证任何人达到了正确」被译成
  「保证没人能达到正确」——本仓最核心的那条分界句，意思正好反过来。
- **gate-lint 与 shell-lint 只扫 `scripts/`，扫不到仓根**：`install.sh` 在
  `GOVERNED` 里、是使用者跑的第一个脚本，它那 4 处光秃秃的 `die` 一处没查到。
  扫描范围改成整个包，样本排除写成相对本次 SCAN 的前缀（写死 `*/fixtures/*`
  会让样本把自己排除掉——9 个用例盯着这一点）。
- `sop-first.md` 的窗口写成「从 bad 那行起 4 行内」差了一格，脚本是 5（含 bad 自己）。
- 「规则清单」在消费项目里也跑（比对的是装进去的那份副本），gate skill 说反了。
- `machine-first.md` 对 lkmm 对照组的描述停在「全局数一条 Sometimes」的旧口径。
- 「分工判据」原本写在三处且已开始各说各的，正文留 CLAUDE.md，README 链过去；
  `design-doc-discipline` 与 `writing-economy` 逐字重复的两行删掉一处。
- README「里面有什么」表漏了 `rules/`，而两行之后就说规范正文是 `CLAUDE.md` + `rules/`；
  安装段与 en/ja 说的不是一回事（它们用安装器仓，zh 只写了手动路径）。
- en/ja 的 `CLAUDE.md` 外链到 README 的节名在译本里不存在；两仓 CHANGELOG 的
  0.0.23 还写 51 例。
- `skills/` 与 `templates/` 逐字节复制进译本仓、内容是中文，与「用日文的不必读中文」
  冲突。**没修，按 GLOSSARY 的办法把缺口显式写在文件里**——补法是让它们进 MANIFEST
  逐篇翻译，那是设计判断。

**第三批：126 个变异跑出来的判别力盲区**（用例 69 → 111）

- **`gate.sh` 自己此前零覆盖**，而它是整道门禁唯一的判决点：把 `run_stage` 改成
  无条件记 PASS，造一处真实文档违规，**退出码是 0**、自检全绿。补法是把 `gate.sh`
  连同 `lib.sh` 拷进临时包、子脚本换成桩——测的是记录与汇总的判读，不递归。
  现在钉住「全绿 ⇒ 退出码 0 且阶段清单齐全」与「任一阶段红 ⇒ 门禁红」（7 个阶段各一条）。
- **`GOVERNED` 9 项只有 2 项有人盯**：其余 7 项可静默摘掉，`VERSION` 的整行匹配
  写成 `grep -q` 后新增一个 `VERSIONING.md` 就能冒充抬过版本。逐项补样本。
- **`i18n-sync` 8 条检查只钉住 1 条**，其中三条在脚本注释里被标成「实测过的回归」——
  清单陈旧、GLOSSARY 漂移、抄了清单没重译，都可以再犯一次。8 条分支各补一例。
- **`lkmm` 的配对判据可以被静默改回旧 bug**（全局数一条 Sometimes 就算），
  补「3 条无关 Never + 1 条无关 Sometimes」的样本。
- **`check.sh` 零用例**、**`lib.sh` 的 gawk 与 LC_ALL 守卫零用例**、
  **`manifest` 的 `CLAUDE.md` 覆盖零用例**、`show-me-test` 的未跟踪文件扫描零用例，
  各补齐。摘掉 LC_ALL 守卫后 C locale 下编号检查会把每处正确引用都误判成
  「括注没闭合」——这类「悄悄改判」比漏检更难发现。
- **一个样本触发多类违规、却只有一条 `want`**：`原先`/`以前`/`之前` 三条模式共用
  一句 why，删任一条都不红——把 why 拆开，一条一句。`pkill -f` 与 `killall`
  拆成两个样本。
- **豁免也要有人盯**（方向相反的盲区：门禁开始误拒，人就会绕过它）：注释里的 `bad`、
  bad 与 howto 之间的注释、历史节里按规则写的「曾经 X / 现在 Y」、heredoc 里的赋值、
  与外层同名的 `local` 变量、在 `$( )` 里设了外面不读的全局——各配一个踩在边界上的绿样本。
- 复跑那 126 个变异：**门禁察觉不到的从 55 降到 6**，其中 5 个验过是等价变异
  （按 `test-discipline.md` 分开记，不补样本），剩下 1 个补掉——`lib.sh` 里 `die`
  少第二个参数时的运行期兜底。gate-lint 在调用点静态拦裸 `die`，但它自己写明有几种
  形态「认不出、不判」，那几种只剩这道兜底，而兜底此前没人盯。用例 111 → 112。
- 规则增补：sop-first 的出路要求覆盖 `die`；design-doc-discipline 给检查代码的
  坑注释开例外；kb-discipline 写明 `INDEX.md` 不要历史节；
  command-safety 标出哪两条已经是检查。

## 0.0.23 — 2026-09-02

对抗审计后的整批门禁修复（审计报告见当轮会话记录）：

- doc-lint：围栏未闭合判红；「历史版本」之后再开 `##` 小节判红；不变量定义
  不再认历史节里的表格；rules 文件留历史节判红；rule-definition 标记限定
  CLAUDE.md / rules / skills 三处；not-numbers 豁免已登记编号判红；
  「脚本文件」「同上游」这类构词不再误判为指代（首字排除）。
- gate-lint：bad 认全形态（单双引号、变量、`;{|&`/`then`/`else` 之后）；
  注释里的 howto 字样不算出路；汇总豁免收紧为「失败/未通过/未过：$计数」。
- Show me test 拆成 show-me-test.sh：注释里的 `#[test]` 不算；tests/ 只认 `.rs`；
  build.rs 也是代码；在默认分支上基准退到 HEAD~1，先提交再跑门禁不再「无对象可判」。
- lkmm：每条 Never 必须有同名 `-nofence` 配对对照，全局数一条 Sometimes 不再算数；
  静态检查前置到 herd7 探测之前；全部拒绝补 howto。
- i18n-sync / manifest：失败分支的 diff 诊断管道在 set -e + pipefail 下会把脚本
  中途带走（howto 与后续语言全丢）——补 `|| true`；三语同步前先验清单新鲜度；
  GLOSSARY.md 并入共享区同步。
- 新增 version-discipline.sh：改规范本体不抬 VERSION 直接红（原是提醒句）。
- selftest 泛化到全部门禁脚本：doc-lint / gate-lint / lkmm 样本目录 +
  show-me-test / version-discipline / manifest / i18n-sync 脚本化用例，共 54 例。

## 0.0.22 — 2026-09-02

- session-wrapup 增补「同一个仓里有没有别的会话在飞」一节；show-me-test 增补
  变异清单；test-discipline 增补「确定性模型 N 轮」「变异测试证明的是断言会红」；
  GLOSSARY 新增变异测试相关术语。

## 0.0.21 及更早

见 `git log --oneline`——每条提交信息都按「版本：改了什么」写。
