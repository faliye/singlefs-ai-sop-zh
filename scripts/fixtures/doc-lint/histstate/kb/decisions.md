# 决策

节点大小 16K（原为 4K）。

写路径原先走两次落盘，现在只走一次。

挂载点以前叫 mountpoint。

回收策略之前叫 sync-reclaim。

这条路子曾经是可行的。

~~旧的分配器~~ 换掉了。

〔已废弃〕的那套编码不再使用。

D1 已被 D22 覆盖。

这一格此前写的是 0.069。

那一列此前写着 32 位。

正文此前写成两项未定。

本决策此前只管落盘。

这条不变量此前从未写进清单。

这个阶段此前没有写过样本。

那条判据此前没写过。

I-8.1 此前不存在。

这个字段曾经写作 generation。

那个常量曾经写着 4096。

块大小原先写的是 4 KiB。

95 已经不是现行头部宽度了。

## 历史版本

### 2026-08-29
- 建档。
