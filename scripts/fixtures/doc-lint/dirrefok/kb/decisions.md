# 决策

## D1 数据可移动性 —— 已定

四条负载是那条公式在它上面的四个点。
多层树下面的单元不读；每一节标题下面一行写它属于哪一步。
4 KiB 以上的写走另一条路，水位以下的记录即死。
例如下一次发布时再判。
那个 `min` 上方的注释只讲原子性，字段表下方另有口径。
上下文窗口有限；把块挂上文件系统之后再读。
开放段写满就开到下一段，bump 到下一节点，程序读下一行之前先转打。
前面三个取样点与后面几轮一致。
往下表达一个意思；溢出段放在它下面，D1（数据可移动性）第 2 条下面另有注。

## 历史版本

### 2026-09-17
- 建档。
